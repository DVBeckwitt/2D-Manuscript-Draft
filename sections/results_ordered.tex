\section{Results: ordered films}

We first applied the staged workflow to ordered MBE-grown Bi$_2$Te$_3$ and Bi$_2$Se$_3$ films. The detector images contained the specular features and off-specular arcs expected for a 2D oriented powder with little stacking disorder. These two materials therefore provided the ordered-film benchmark.

\begin{figure}[H]
  \centering
  \maybeinclude[width=\linewidth]{figures/results_ordered/ordered_detector_summary.png}
  \caption{Ordered-film detector-space fits. (a) Bi$_2$Te$_3$ measured, simulated, and residual detector views after the final staged refinement. (b) Corresponding Bi$_2$Se$_3$ detector comparison. (c) Representative local profiles from specular and off-specular detector ROIs used to check the mosaic/profile state after center alignment. (d) Post-fit $Q_z$ intensity checks for ROI-integrated Bragg intensities in the two ordered films. The comparison is made after the detector-space geometry and local line-shape terms have been fixed.}
  \label{fig:ordered_detector_fits}
\end{figure}

Figure~\ref{fig:ordered_detector_fits} summarizes the final ordered-film fits in the same order as the refinement workflow. The measured, simulated, and residual detector panels show that the model placed the specular peaks and off-specular arcs at the observed detector positions for both Bi$_2$Te$_3$ and Bi$_2$Se$_3$. This is the most sensitive check of the beam-center, detector-distance, detector-tilt, and sample-alignment parameters, because even small geometric errors would produce peak-position offsets across the image.

With the geometry and mosaic/profile state fixed, the structure-factor stage was allowed to vary the crystallographic parameters that enter Eq.~\eqref{eq:structure_factor}: lattice constants, site coordinates, occupancies, and anisotropic displacement terms. The preliminary fitted values used for the simulated panels in Fig.~\ref{fig:ordered_detector_fits} are summarized in Table~\ref{tab:ordered_sf_parameters}. The fitted values remain inside the literature spread of the structural priors for Bi$_2$Se$_3$ and inside the combined MBE-lattice and bulk-neutron prior envelope used for Bi$_2$Te$_3$.\cite{Trueblood1996,Mansour2014,Steiner2014,Callaert2019}

\begin{table}[H]
  \centering
  \caption{Preliminary ordered-film structure-factor parameters used in the simulated detector panels of Fig.~\ref{fig:ordered_detector_fits}. Literature comparisons define the acceptable spread for the fitted lattice, coordinate, occupancy, and displacement parameters.}
  \label{tab:ordered_sf_parameters}
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{1.12}
  \begin{tabularx}{\linewidth}{@{}p{1.55cm} >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X@{}}
    \toprule
    Material & Lattice parameters & Site coordinates & Occupancies & Directional displacement terms \\
    \midrule
    Bi$_2$Te$_3$ &
    $a=4.3829\,\text{\AA}$, $c=30.486\,\text{\AA}$; consistent with the relaxed MBE limit and 300 K bulk reference values.\cite{Mansour2014,Steiner2014} &
    $z_{\mathrm{Bi}}=0.40042$, $z_{\mathrm{Te1}}=0$ fixed, $z_{\mathrm{Te2}}=0.20957$; within the reported coordinate uncertainties of the neutron-prior convention.\cite{Mansour2014} &
    $o_{\mathrm{Bi}}=o_{\mathrm{Te1}}=o_{\mathrm{Te2}}=1.000$ for the stoichiometric working model.\cite{Steiner2014,Gharsallah2016} &
    $U_r/U_z=0.0186/0.0244$ for Bi, $0.0163/0.0129$ for Te1, and $0.0264/0.0144\,\text{\AA}^2$ for Te2; fitted ADP values bracketed by the bulk neutron priors.\cite{Mansour2014,Trueblood1996} \\
    Bi$_2$Se$_3$ &
    $a=4.14072\,\text{\AA}$, $c=28.6561\,\text{\AA}$; within the parent PXRD anisotropic-model spread.\cite{Callaert2019} &
    $z_{\mathrm{Bi}}=0.40055$, $z_{\mathrm{Se1}}=0$ fixed, $z_{\mathrm{Se2}}=0.21010$; within the reported Bi and outer-Se coordinate uncertainties.\cite{Callaert2019} &
    $o_{\mathrm{Bi}}=1.000$, $o_{\mathrm{Se1}}=1.000$, and $o_{\mathrm{Se2}}=0.989$; consistent with the outer-Se partial-occupancy prior.\cite{Callaert2019} &
    $U_r/U_z=0.0036/0.0264$ for Bi and $0.0046/0.0485\,\text{\AA}^2$ for both Se sites; the equal Se1/Se2 ADPs follow the source refinement constraint.\cite{Callaert2019,Trueblood1996} \\
    \bottomrule
  \end{tabularx}
\end{table}

The local profile checks then isolate line-shape information from integrated intensity information. The specular-cap and off-specular-arc profiles were reproduced by the same Gaussian-plus-Lorentzian mosaic profile. The final $Q_z$ checks compared the ROI-integrated Bragg intensities after the geometry and line-shape parameters had been fixed. Agreement in this view indicated that the ordered structure-factor description was sufficient for these films within the resolution of the present workflow. The important result is that the same refinement remained stable in both systems, thereby setting the baseline for the PbI$_2$ disorder analysis.
