\subsection{Mosaicity}
\label{sec:mosaicity}
The mosaic picture is most easily understood by comparing the three orientational limits introduced in Fig.~\ref{fig:intro_single_recip}, Fig.~\ref{fig:intro_3d_recip}, and Fig.~\ref{fig:intro_2d_recip}. For a perfect single crystal (Fig.~\ref{fig:intro_single_recip}), a reflection carries its full weight at a single Bragg spot. For a 3D powder (Fig.~\ref{fig:intro_3d_recip}), random crystallite orientations spread that weight uniformly over the Bragg sphere, so the Ewald-sphere intersection gives a circle of equivalent reciprocal-space points and associated exit vectors $\mathbf{k}_f$. A 2D powder (Fig.~\ref{fig:intro_2d_recip}) lies between these limits: the allowed $\mathbf{k}_f$ values still lie on that circle, but out-of-plane mosaicity makes some portions of the circle more probable than others. Mosaicity is therefore treated as a redistribution of the ideal per-reflection intensity over the allowed \(\mathbf{k}_f\) family, rather than as a change to the underlying diffraction condition or structure factor.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.35\linewidth]{figures/mosaic/2d_mosaic.png}
  \caption{Schematic of crystallite layer-normal misorientation (mosaicity) about the surface normal. A distribution of crystallite orientations may form in a thin film.}
  \label{fig:mosaic}
\end{figure}
For a reflection, we work in the meridional section (\(G_r, G_z\)) of reciprocal space, where \(G_r\) is the projection of the reciprocal lattice vector onto the plane of the substrate, and \(G_z\) is the component along the surface normal. In this section, the perfect-crystal Bragg spot appears as a point on the Bragg circle. In the full three-dimensional picture, random in-plane rotations generate the circle of equivalent reciprocal-space intersections and associated \(\mathbf{k}_f\) values described above, consistent with the reciprocal-space evolution from Fig.~\ref{fig:intro_single_recip} to Fig.~\ref{fig:intro_3d_recip} to Fig.~\ref{fig:intro_2d_recip}. Out-of-plane misorientation then replaces the uniform weighting of that ideal circle by a line density \(I_0 \omega(\Delta\theta)\), where \(I_0\) is the total intensity associated with the reflection before mosaic redistribution.

We describe this out-of-plane mosaicity by a misorientation density \(\omega(\Delta\theta)\), where \(\Delta\theta\) is the crystallite tilt angle relative to the substrate normal (\(\Delta\theta=0\) for perfect alignment). Because \(\Delta\theta\) is an angular variable, we treat \(\omega\) as a \(2\pi\)-periodic wrapped distribution: \(\Delta\theta\) and \(\Delta\theta+2\pi\) are equivalent, and without wrapping the tails would be artificially truncated at the chosen angular boundary (see supporting information, Sec.~S1.1). We model \(\omega\) as a unit-normalized sum of a Lorentzian and a Gaussian:
\begin{equation}
\omega(\Delta\theta)=\eta\,\frac{1}{\pi}\frac{\Gamma}{\Delta\theta^{2}+\Gamma^{2}}
+(1-\eta)\,\frac{1}{\sqrt{2\pi}\sigma}\exp\!\left(-\frac{\Delta\theta^{2}}{2\sigma^{2}}\right),
\qquad 0\le\eta\le 1,
\end{equation}
with Lorentzian half-width at half maximum \(\Gamma\) and Gaussian standard deviation \(\sigma\).
These components are related to the full reciprocal lattice vector \(\bm G = (H, K, L)\), but the relationship depends on the crystal symmetry. For a general crystal, \(G_r\) is defined as the magnitude of the projection of \(\bm G\) onto the plane, and \(G_z\) is the component along the normal direction. A central question for the specular geometry is how a reflection can contribute intensity even when the incident angle is detuned from its nominal Bragg condition. More specifically, the model must account not only for the existence of off-condition specular intensity, but also for how that intensity remains visible across the sequence of specular reflections as the incident angle is varied. The Lorentzian component is introduced here as a possible source of the long tails required for that later discussion.

Figure~\ref{fig:mosaic_combo} summarizes how this reciprocal-space broadening appears in the meridional $\{G_r,G_z\}$ plane and how it maps onto characteristic detector-space features for in-plane and specular conditions.
\begin{figure}[H]
  \centering
  \captionsetup[subfigure]{list=false,labelsep=none}

  % --- top row: 2D schematic Bragg-circle views ---
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \paneltagged{(a)}{\input{figures/mosaic/mosaic_bragg_inplane}}
    \phantomcaption
    \label{fig:mosaic_combo:schem_inplane}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \paneltagged{(b)}{\input{figures/mosaic/mosaic_bragg_specular}}
    \phantomcaption
    \label{fig:mosaic_combo:schem_specular}
  \end{subfigure}

  \vspace{0.6em}

  % --- bottom row: corresponding detector images ---
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \paneltagged{(c)}{\includegraphics[width=0.85\linewidth]{mosaic/inplane_detector_band.png}}
    \phantomcaption
    \label{fig:mosaic_combo:img_inplane}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \paneltagged{(d)}{\includegraphics[width=0.85\linewidth]{mosaic/specular_detector_cap.png}}
    \phantomcaption
    \label{fig:mosaic_combo:img_specular}
  \end{subfigure}

  \caption{Mosaic broadening viewed in the $\{G_r,G_z\}$ plane (a,b) and its detector-space manifestation (c,d). Intensity is redistributed along the Bragg circle with line density $I_0\,\omega(\Delta\theta)$. The in-plane condition with $G_r\neq 0$ (a,c) yields two arcs of reflection, while the specular reflection with $G_r=0$ (b,d) concentrates intensity near the polar cap, forming a single arc.}
  \label{fig:mosaic_combo}
\end{figure}
Random in-plane rotations about the film normal revolve the meridional line density about the \(G_z\) axis, generating an axially symmetric distribution on the Bragg sphere. Let \(\vartheta\) denote the polar angle on that sphere, and let \(\vartheta_B\) denote the Bragg position for the reflection, so that the mosaic misorientation is \(\Delta\theta=\vartheta-\vartheta_B\).

We normalize the corresponding Bragg-sphere surface density \(\sigma(\vartheta)\) so that \(\int \sigma(\vartheta)\,dA = I_0\), yielding
\begin{equation}
\sigma(\vartheta)=\frac{I_0}{2\pi R^2\sin\vartheta}\,\omega(\vartheta-\vartheta_B)  \qquad 0<\vartheta<\pi,
  \label{eq:mosaic_sigma_maintext}
\end{equation}

where \(R\) is the Bragg-sphere radius for the reflection. The factor \(1/\sin\vartheta\) is the Jacobian for converting the one-dimensional tilt distribution \(\omega\) into a surface density. This surface density supplies the weights used to distribute the ideal intensity \(I_0\) over the neighboring \(\mathbf{k}_f\) rays generated by misoriented crystallites.

The reciprocal-space consequences appear on an area detector in
Fig.~\ref{fig:mosaic_pbi2}. Strong out-of-plane mosaicity concentrates specular
intensity near discrete $G_z$ values at $G_r=0$ (region A), while random
in-plane orientation distributes in-plane reflections uniformly in azimuth
about $G_z$, producing ring-like features (region B).

\begin{figure}[H]
  \centering
  \includegraphics[width=0.3\linewidth]{mosaic/pbi2_waxs.png}
  \caption{Log-scale WAXS pattern of Bi$_2$Se$_3$ at $\theta_i=5^\circ$. Region A: specular reflections. Region B: in-plane scattering.}
  \label{fig:mosaic_pbi2}
\end{figure}

In the in-plane region (Fig.~\ref{fig:mosaic_pbi2}B), the Ewald sphere intersects the broadened Bragg-sphere feature along an extended band. Azimuthal averaging then produces band-like scattering whose radial width and line shape are set primarily by the core of $\omega$ and by the Jacobian factor $1/\sin\vartheta$ in Eq.~\eqref{eq:mosaic_sigma_maintext}.
In the specular region (Fig.~\ref{fig:mosaic_pbi2}A), the same mosaic spread produces cap-like broadening about the specular reflections. Off-condition specular intensity arises from crystallites whose layer normals are tilted by \(\Delta\theta\) away from the mean, so the intensity away from the nominal condition is governed primarily by the tails of \(\omega\), with only weak variation from the geometric factor in Eq.~\eqref{eq:mosaic_sigma_maintext}. Within this picture, a single incident \(\mathbf{k}_i\) can receive contributions from multiple specular reflections, including reflections whose nominal \(\theta_i\) conditions are not exactly satisfied.