\subsection{Mosaicity}
\label{sec:mosaicity}

Mosaicity can be understood by comparing the orientational limits introduced in Fig.~\ref{fig:intro_2x3}. For a perfect single crystal, a reflection carries its full weight at a single Bragg spot. For a 3D powder, random crystallite orientations spread that weight isotropically over the Bragg sphere. A 2D powder lies between these limits: in-plane rotations are randomized about the film normal, while the layer normals retain a finite out-of-plane misorientation distribution. Mosaicity is therefore treated as a redistribution of the ideal per-reflection intensity over the allowed $HK$ family.

For a reflection, we work in the $(G_r,G_z)$ cross-section of reciprocal space, where $G_r$ is the in-plane radial component. Random in-plane rotations generate a ring of equivalent reciprocal-space intersections about $G_z$. Out-of-plane misorientation then gives a nonuniform line density around that ring, written as $I_0\omega(\Delta\theta)$, where $I_0$ is the reflection intensity before mosaic redistribution and $\Delta\theta$ is the crystallite tilt relative to the mean layer normal.

The ordered-film comparison below requires both a narrow mosaic core and a long tail. We therefore describe the out-of-plane mosaicity by a normalized Gaussian-plus-Lorentzian angular distribution, evaluated in wrapped form on $(-\pi,\pi]$ to conserve probability over one full angular period and avoid a discontinuity at $\Delta\theta=\pm\pi$. The Gaussian and Lorentzian widths are independent. The explicit wrapped Gaussian, wrapped Lorentzian, and surface-density forms are deferred to the supporting information.

Figure~\ref{fig:mosaic_combo} summarizes how this reciprocal-space broadening appears in the $\{G_r,G_z\}$ plane and how it maps onto characteristic detector-space features for in-plane and specular conditions.

\begin{figure}[H]
  \centering
  \captionsetup[subfigure]{list=false,labelsep=none}

  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \paneltagged{(a)}{\input{figures/mosaic/mosaic_bragg_inplane}}
    \phantomcaption
    \label{fig:mosaic_combo:schem_inplane}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \paneltagged{(b)}{\input{figures/mosaic/mosaic_bragg_specular}}
    \phantomcaption
    \label{fig:mosaic_combo:schem_specular}
  \end{subfigure}

  \vspace{0.6em}

  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \paneltagged{(c)}{\includegraphics[width=0.85\linewidth]{mosaic/inplane_detector_band.png}}
    \phantomcaption
    \label{fig:mosaic_combo:img_inplane}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \paneltagged{(d)}{\includegraphics[width=0.85\linewidth]{mosaic/specular_detector_cap.png}}
    \phantomcaption
    \label{fig:mosaic_combo:img_specular}
  \end{subfigure}

  \caption{Mosaic broadening viewed in the \(\{G_r,G_z\}\) plane (a,b) and its detector-space manifestation (c,d). Intensity is redistributed along the Bragg circle with line density \(I_0\omega(\Delta\theta)\). The in-plane condition with \(G_r\neq0\) (a,c) yields two off-specular arcs, while a specular reflection with \(G_r=0\) (b,d) concentrates intensity near the polar cap. The long tail of \(\omega(\Delta\theta)\) is what gives finite intensity to $HK=0$ specular-family peaks that are not directly excited by the nominal incidence angle.}
  \label{fig:mosaic_combo}
\end{figure}

After random in-plane rotation, the one-dimensional tilt density becomes an axially symmetric surface density on the Bragg sphere. This density supplies the ray weights used to distribute $I_0$ over mosaic-broadened exit directions. 
