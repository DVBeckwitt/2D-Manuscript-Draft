\section{Results: diffuse scattering in PbI\texorpdfstring{$_2$}{2}}
\label{sec:results_pbi2_deferred}

% This section is intentionally deferred from the active ordered-film manuscript.
% Dr. Maselli's guidance was to finish the Bi2Se3/Bi2Te3 line-shape validation first.
% Reintroduce PbI2 only after the ordered-film $Q_z$ projection figures are complete.

PbI$_2$ is the disorder-sensitive extension of the same framework, not the first proof of the method. In the present draft this material is held out of the main text so that the ordered Bi$_2$Se$_3$ and Bi$_2$Te$_3$ validation remains the central result.

When this section is reintroduced, it should appear as a short extension built around a selected-rod figure: measured intensity, ordered-baseline calculation, stacking-disorder calculation, and residuals projected along the same fixed-$Q_r$ trajectory and reported versus $Q_z$. The figure should not be added until the corresponding PbI$_2$ data product is ready and until it no longer competes with the ordered-film demonstration.
