# Changes integrated from annotated PDF (2026-06-25)

1. Rewrote the low-L sentence in `sections/mosaicity_texture.tex` to identify
   the near-origin intensity as reflectivity and to explain why it remains in
   the crop.
2. Corrected the malformed `m\neq=0` expression to `m\neq 0` and placed both
   comparison cases in math mode.
3. Changed the main-text terminology from an optical-to-structural handoff to an
   optical-to-kinematic handoff, with matching terminology in the Supporting
   Information.
4. Replaced the former blend-centered Figure 8 with a single overlaid plot of
   the measured Bi2Se3 near-critical `m=0` profile, the Parratt calculation, and
   the finite-stack kinematic calculation. The internal blended curve remains
   part of the calculation but is not plotted.
5. Added reproducible figure inputs and a generation script under
   `figures/correlated_effects/`. Because the source archive did not include the
   original arrays, these inputs are digitized from the plotted curves in the
   annotated PDF and should be replaced by direct array exports before final
   submission.
