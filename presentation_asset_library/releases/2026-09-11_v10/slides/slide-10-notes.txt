Bi₂Se₃: measured and simulated

Suggested pace: 25 seconds.

Here is Bi₂Se₃. Measurement and simulation share the crop and scale. The central calculation joins low-Qz reflectivity to diffraction using conditional optical settings. The retained mosaic and structure stay fixed. This update is not a new joint fit. Compare the central response and paired off-axis features, including the visible differences.

Sources and figure interpretation
- Frozen physical-model stage: retained September 3 model. Fresh full beam Monte Carlo uses 4096 initial incident states and 4096 mosaic draws per state. Geometry, CIF structure, mosaic parameters, count scale and background remain fixed. These are display rerenders, not a fresh structural fit.
- Direct arrays and final render manifest: build_codex/slate_talk/ordered_render/bi2se3_n4096_m4096/. Source panels are measured_panel.png and calculated_panel.png. The current generic-CIF forward sampler replays the archived SLATE-rMC-bi2x3-polar-line-refit model.
- Measured and calculated panels retain the same display normalization within each pair. Different materials need not share the same normalization. Empirical background is disclosed in the panel title.
- The 5°, 10°, and 15° ordered-film conditions entered staged refinement. These images are fitted comparisons, not independent predictions. The later September 9 profiles appear separately on the matched-profiles slide.

Retain the complete direct-array, frozen September 3 stage, source-count, shared-scale, empirical-background and fitted-condition source notes. The later profile comparison follows both detector images, not immediately after this slide.

Retain the complete direct-array, frozen September 3 stage, source-count, shared-scale, empirical-background and fitted-condition source notes. The later profile comparison follows both detector images, not immediately after this slide.

V8 updates the image realization: retain the September 3 frozen diffraction core and combine it with compatible September 5 conditional Parratt optical-stitch settings for the axial r=0 response. The full r=0 contribution is evaluated from the continuous deterministic model, while off-axis diffraction uses Monte Carlo sampling. This supersedes the historical whole-image Monte Carlo description retained in earlier source notes. Final image-specific sampling and array provenance are supplied separately by the renderer. No new joint fit, fitted thickness, structural parameter or acceptance claim is made. Keep the empirical-background disclosure, matched crop/scale and direct-array provenance. The fixed mosaic/structure-factor parameter table remains applicable to the frozen core; it is not a complete report of the additional conditional optical settings. Final image sources: C:\Users\Kenpo\Nextcloud\Git Projects\2D-Manuscript-Draft\build_codex\slate_talk\animated_deck_v8\ordered_render\bi2se3\detector_comparison_arrays.npz; manifest C:\Users\Kenpo\Nextcloud\Git Projects\2D-Manuscript-Draft\build_codex\slate_talk\animated_deck_v8\ordered_render\bi2se3\manifest.json. The same 4096 actual incident states supply both components. Nonzero-r diffraction uses 16384 Monte Carlo draws per incident state, while the full r=0 response is integrated directly from the local-lamella Parratt-to-kinematic model over the same valid detector-pixel areas. The count scale, structure, mosaic, source distribution and empirical background are retained. No independent per-panel normalization or image smoothing is used. Measured and calculated arrays share 6-by-6 native-pixel means, rows 0 through 1601 and columns 0 through 2999. The common asinh display caps at the measured 99.9th percentile to show weak features; the saved numeric arrays are unclipped. Optical thickness is 500 angstrom, bottom roughness 10 angstrom, and top roughness 5.3555911321161505 angstrom, from the compatible September 5 conditional optical model. This is not a new joint refinement or a measured thickness.
