Bi₂Te₃: termination changes near 003

Suggested pace: 30 seconds.

The same interior can have different exposed end planes. For Bi₂Te₃, teal changes only their mixture. Mosaic, beam, scale and background stay fixed. The excess wing almost disappears, while the core error changes little. Purple also refits mosaic and scale, improving the core. The r-equals-one peak barely changes with endings alone. These are conditional trial weights. Compare Bi₂Se₃ using the same ranges and calculation stages.

Playback cue
Introduce the end-plane illustration, then press Next once. The illustration fades out over 400 milliseconds to reveal the editable comparison charts. Advance the slide manually after discussing the comparison.

Sources and figure interpretation
- Material Bi₂Te₃, nominal 5-degree incidence. Both materials use the same archived all-material analysis stage: C:\Users\Kenpo\.codex\visualizations\2026\09\06\01a0790a-e521-7951-8cf8-97659242b079\all_material_surface_refit.ra_diag.npz (SHA256 e8b9b1c5a015a15f65bf1ab2d2e6bad21176c23f775ff0eb64ee1082d60d8b2b). Manifest C:\Users\Kenpo\.codex\visualizations\2026\09\06\01a0790a-e521-7951-8cf8-97659242b079\all_material_surface_refit.json (SHA256 f46ebb41cc665443f3f5605a3319cf618d9cdbf2864f2a9164956be592b2f9f5). This replaces the previous presentation's earlier Te equal-A/B sensitivity example. It is a separate stage from the preceding detector images and matched off-specular profiles.
- Top means the positive-c end before mosaic rotation. The baseline, code W, exposes Bi at the top and central Te at the bottom. Code A exposes Bi / outer-Te. Code B exposes outer-Te / Bi. Central and outer refer to the positions in the quintuple-layer sequence outer-X / Bi / central-X / Bi / outer-X. Both surface and interface fragments can change. Interior structure, composition and atom count remain fixed.
- Exact conditional incoherent intensity fractions [W,A,B] = [0.024450479184432883, 0.9755495208152927, 2.744001846504022e-13]. These are component weights imported from the saved joint trial. They are not confidence levels or measured compositions. The minor omitted component is below 1e-7 percent. No domain size, coherence length or lateral arrangement is determined.
- Baseline orange dashed curve: archived exact W response times the prior common scale, plus the fixed background. Teal Ends only: the saved trial fractions applied to exact W/A/B component responses with the prior mosaic and scale, plus that same background. Purple Joint refit: the direct archived trial profile with changed mixture, mosaic and common scale. Black markers and thin connecting lines: measured native detector-region means. All calculations retain the same observations, detector regions, beam distribution, Cu doublet, optics and background. No smoothing, refitting, independent normalization or digitization was applied here.
- The calculation uses all 256 archived source states and native detector quadrature order 4. Source-specific convergence near folds remains unqualified. Both material counts retain their own archived acquisition/model scale. Identical plotted count axes do not imply normalized intrinsic scattering strengths.
- Shared horizontal coordinate: Nominal L = |Q|/|c*|, derived from archived two-theta bin centers and the nominal wavelength. It is not exact sample Qz/|c*|. The latest source PNG called this Projected L and its secondary axis Projected Qz; the presentation corrects those labels using export_inputs.py:165 and execution_fit_surface_all_py:165. Direct two-theta coordinates are retained in the private exports.
- Identical display and error windows for both materials: core 2.9 <= Nominal L < 3.2, wing 3.2 <= Nominal L < 4.5. Actual bins only, with no interpolation. Bi₂Te₃ has 9 core bins and 38 wing bins. Mean squared error is the unweighted mean of squared differences between model and measured native-pixel means within each region. Each percentage is 100*(MSE_variant/MSE_baseline - 1), rounded to one decimal on the slide. These are descriptive local errors, not significance tests or the optimizer's weighted objective.
- Common-window core MSE change: endings only -2.47610534494 percent, joint -85.5613855878 percent. Wing: endings only -99.4790992883 percent, joint -97.8323241448 percent. The source chat used material-specific masks, so its numerical percentages differ slightly. Those original masks and metrics are retained in the direct export for traceability.
- The r=1 control at nominal L=4.9 has net calculated intensity change -0.00110396973761 percent from endings alone. This nonzero-hk peak is not purely hk0. Exact integer crystal-frame Bragg amplitudes, including hk0, are invariant for these cuts, but finite detector bins and weak wings need not be. Mosaic and scale refitting can change that control. The comparison does not establish that all off-axis profiles are termination independent.
- Conditional trial status: 13 of 54 archived checks worsened. Neither joint trial is accepted. Local 003 agreement does not establish global agreement. No significance or unique termination determination is established. Se's dominant ending changed in a subsequent rejected background-stage trial; Te's A preference persisted, but neither outcome is a significance test.
- The physical advantage is sensitivity to atomic endings beyond bulk Bragg peaks alone. Surface X-ray diffraction already probes termination coverage: Ju et al., Physical Review B 103,125402 (2021), https://doi.org/10.1103/PhysRevB.103.125402 .
- Reproducible full-precision source exports, CSV files, source indices and controls: build_codex/slate_talk/termination_comparison/. Native PowerPoint chart snapshots use 12 significant digits for portable Excel precision. Full-precision arrays remain unchanged.

Retain all direct trial-array provenance, identical window definitions, nominal-L convention, conditional mixture definitions and failed-comparison guards from v5. No new termination inference is introduced.

Retain all direct trial-array provenance, identical window definitions, nominal-L convention, conditional mixture definitions and failed-comparison guards from v5. No new termination inference is introduced. Begin with a qualitative end-plane schematic, then reveal the existing chart comparison with a native fade on the first click. Spend approximately 3 to 5 seconds introducing the same interior and different exposed end planes within the existing 30-second budget. The schematic explains structural choices only. Do not transfer the older explorer’s illustrative mixture, fit stage, metrics or color assignments into the retained comparison. The plotted curves and conditional trial weights remain the original v6 direct-array evidence.
