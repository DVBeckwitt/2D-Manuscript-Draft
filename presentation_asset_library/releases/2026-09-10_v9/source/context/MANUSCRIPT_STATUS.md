# Manuscript Status

Last updated: 2026-09-10

This is the canonical manuscript guidance and status document for the 2D diffraction / mosaicity manuscript. It subsumes the previous advisor notes, meeting-prep checklists, figure status, supplement status, changelogs, and manuscript task trackers. Use this file as the single source of truth for writing, rewriting, figure planning, caption drafting, structural editing, and manuscript triage.

Former detailed documents are intentionally not required for active work; git history remains the archive for exact old wording.

## Source Context

Primary meeting source: Dr. Paul F. Miceli / David manuscript discussion on 2026-04-30.

No next advisor meeting date is currently known. The earlier planned date, 2026-05-07, is past.

Current manuscript priority: Bi2Se3 and Bi2Te3 ordered-film figures and physics narrative.

Source context used during current revisions:

| Item | Role |
|---|---|
| `Voice 260430_141619_llm.txt` | Transcript/source notes from the 2026-04-30 Miceli meeting. |
| `2D-Manuscript-Draft.zip` | Manuscript repo archive used to identify current section and figure structure. |
| `Combined_variance_report_PbI2_Bi2Te3_Bi2Se3_updated.tex` | Technical/report material available for uncertainty and manuscript cross-checking. |
| `Advisor_Writing_Philosophy.md` | General writing principles distilled from the advisor's repeated guidance. Integrated below and in `AGENTS.md`. |
| `Comment_by_Comment_Response_Plan.md` | Proposed response to the 22 annotations. Independently audited below rather than accepted automatically. |
| `Commented_Manuscript_Paul_Miceli.pdf` | Exact 29-page annotated manuscript used to verify comment wording, authorship, order, and highlighted targets. |
| `output/pdf/main_advisor_comment_color_proof.pdf` | Author-reviewed color proof containing three page-2 annotations addressed in the 2026-07-19 proof-response pass. |
| `main_pfm.pdf` | Current 27-page advisor-annotated manuscript reviewed on 2026-08-06. It contains 12 substantive comments on the Methods and ordered-film figures plus one empty note. |
| `build.zip` and `2D_Supplemental.zip` | Executable-model and manuscript/SI archives used for the independent implementation-parity audit and the present revision. |

Current `main.tex` includes:

```tex
\input{sections/introduction}
\input{sections/modelling_methods}
\input{sections/mosaicity_texture}
\input{sections/correlated_effects}
\input{sections/results_ordered}
\input{sections/refinement_workflow}
\input{sections/results_diffuse_pbi2}
\input{sections/discussion_conclusion}
```

## Overleaf Integration - 2026-09-09

The author-edited export at `C:\Users\Kenpo\Downloads\2D_Manuscript(1)` was compared against both the current working tree and the previous generated Overleaf package. This integration preserves the fuller repository as the generation source. The current-state notes in this section supersede older descriptions of figure numbering, main-text wording, and removed main-text panels below. Earlier advisor audits remain intact as historical traceability, not a new certification of the imported derivation or numerical evidence.

| Area | Integration and current state |
|---|---|
| Main text | Imported the substantive changes to `sections/introduction.tex`, `sections/modelling_methods.tex`, `sections/mosaicity_texture.tex`, `sections/correlated_effects.tex`, `sections/results_diffuse_pbi2.tex`, and `sections/discussion_conclusion.tex`. The author now develops continuous rod intensity, powder cylinders, mosaic orientation, Ewald selection, successive Jacobians, and detector projection, then closes with “Main takeaways.” |
| Existing local work | `sections/results_ordered.tex`, `sections/refinement_workflow.tex`, `2D_Supplemental/SI_ordered_structure_parameters.tex`, and the existing image/generation assets already matched the export's substantive content. They were retained. Thirty-four shared cached figure PDFs were unchanged; all shared embedded raster pixels matched the current repository images. Existing uncommitted changes and the pre-existing deletion of `manuscript_latex.zip` were preserved. A pre-integration snapshot is retained locally under ignored `build_codex/overleaf-import-20260909/before/`. |
| Entry point and order | Retained the editable `main.tex` preamble and live TikZ inputs rather than importing cached-only `Manuscript.tex` or its `main.tex` wrapper. Preserved ordered films -> refinement workflow -> PbI2 as required by the project guidance. The new introduction includes a short matching roadmap and a bounded statement of the displayed ordered-film agreement. Export-only trailing `Page 2` debris was not imported. |
| New main figures | Imported `figures/geometry/ewald_intensity_to_detector_mapping.png` and the revised vector `figures/correlated_effects/fig_specular_parratt_kinematic_003_joint_fit.pdf`. The latter PDF is the active authored asset, not the older same-stem PNG placeholder. No direct arrays or generator for these two new assets were supplied. The specular figure and caption explicitly remain illustrative. |
| Figure-source consistency | Updated the live `figures/geometry/system_geometry.tex` labels to `phi_f`, `beta_D`, and `gamma_D` to match the imported prose. Removed an incorrect claim that this schematic draws the incoming angles. Allowed the mosaic composite to float, resolving a nearly empty page. Retained the now-unreferenced sample-geometry and historical PbI2 figure sources for future generation. |
| Supporting Information | Merged the newer root `Supporting_Information.tex` into canonical `2D_Supplemental/SI_failure_modes.tex`, restoring repository-relative paths. The existing scientific SI remains, with a new `subsec:si_ewald_surface_chart` derivation and the source-state vector definition. The new source-state subscript is explicitly suppressed before the retained single-ray equations. |
| Appended note and reproducibility | Imported all ten supplied files into `2D_Supplemental/verbatim_supplemental_note_source/`. The missing `verbatim_supplemental_note.pdf` is now built locally from the supplied base PDF pages and editable page 13. The note retains its own numbering, with duplicate outer page numbers suppressed. Its optional Figure 8 generator reconstructs display intensity from raster colors, with clipping and contrast stretching disclosed by its original README/caption. The optional Figure 3 generator requires an absent NPZ; it is preserved but is not needed to compile the retained note. These illustrations do not close any direct-array evidence gate. |
| PbI2 scope | Adopted the shorter physical pair-correlation account and the author's four-stage hierarchy: nearly ordered 2H, faulted 2H, 2H+6H, then 2H+6H+4H. The four main-text comparison panels are explicit placeholders. The full transition-matrix derivation remains in SI. The removed historical ordered-baseline numerical table and old disorder-series layout remain recoverable in Git history and the local snapshot; unverified historical fitted values were not reintroduced. |
| Integration repairs | Repaired sentence fragments, bare figure references, prose semicolons/em dashes, and definitions of `N`, `d`, intrinsic `q_z`, projected `Q_z`, and `L_proj`. Restricted the cylinder circumference factor to `Q_R>0`, preserved family-versus-multiplicity language, and distinguished Gaussian source position/divergence from the retained wavelength spectrum. Imported the `NevotCroce1980` bibliography entry. |
| Build support | Added `scripts/build-supplement.ps1`, which builds the appendix dependency and the SI using isolated incremental state. The Overleaf exporter builds and includes the appendix and revised vector figure, and tests check dependency hashes and cache reuse without an obsolete fixed figure count. Narrow `.gitignore` exceptions retain the necessary input PDFs and appendix output. |

Current evidence and advisor gates:

- **A08: IN PROGRESS.** `sec:reflectivity` and `fig:specular_joint_fit_placeholder` now contain the authored illustrative stitched response. Direct measured and complete calculated arrays on the declared common scale remain required.
- **C18: BLOCKED.** The new orientation equations and sensitivity schematic were imported, but categorical broad-tail necessity wording was reduced to a testable hypothesis. The same-fit no-tail and fairly re-optimized Gaussian-only comparisons, residuals, and uncertainty remain required. The revised schematic caption explicitly distinguishes sensitivity from a controlled comparison.
- **Ordered-film provenance: BLOCKED.** The author/current working-tree heading is “Validation,” and it is retained in this synchronization. This heading does not close direct-array, common-scale, residual, or uncertainty requirements. The previous literal-heading C21 acceptance is therefore no longer current.
- **New Ewald-chart implementation parity: CHECK.** The supplied derivation is integrated and typeset, but this synchronization did not rerun the executable scattering model or independently validate the new coordinate/density formulas. The earlier implementation audit must not be read as verification of these newly imported equations.
- **PbI2 hierarchy: IN PROGRESS.** The revised main-text scope and explanation are integrated. Direct data/model comparisons and fitted hierarchy parameters remain pending.

Verification: the final main PDF has 24 pages and the complete SI has 38 pages, including the 16-page appended note. Main and SI builds have no undefined references/citations or overfull boxes. Main pages and SI contact sheets were visually reviewed, with focused checks of changed figures, equations, and appendix insertion. The isolated Overleaf compile check and dependency/cache integration tests pass. Final review outputs are `main.pdf`, `2D_Supplemental/SI_failure_modes.pdf`, and `build_overleaf/overleaf.zip`.

## Research Presentation - 2026-09-09

Created `output/presentations/BiX_Research_Presentation_v2.pptx` for a research discussion with the collaborators who grew the Bi2Se3/Bi2Te3 films. The deck contains ten main slides with approximately 17 minutes of speaker notes and five technical backup slides. It follows observation, orientation geometry, wavelength/mosaic distinction, matched detector integration, material-specific profile comparisons, staged calibration, and a proposed controlled growth series. PbI2 remains a backup extension.

The figures retain the original measured/calculated curve pixels. Profile panels are enlarged using faithful crops, with original shared tick strips retained and supplementary geometry schematics rendered from their vector sources. Slide text and the two workflow/evidence tables are editable. Source citations and scientific limitations are included in speaker notes. No personal attribution, growth recipe, new fitted value, or direct-array evidence was invented. Presenter name, affiliation, and individual contributions remain unspecified pending author input.

Verification: the 15-slide PowerPoint passes package, font, table, and slide-geometry checks without findings or warnings. The exact final file was imported and rendered, all slides were visually reviewed, and the revised scientific wording received a separate content review. This verifies the presentation artifact, not any remaining scientific evidence gate. C18, ordered-film provenance, the direct-array reflectivity comparison, and the imported Ewald-equation implementation check retain their existing status. Private generation assets and review renders are under `build_codex/employer_presentation/`.

## Eight-Minute Project Presentation and Beam-Position Integration - 2026-09-09

Created `output/presentations/Oriented_Powder_8min_Draft.pptx` at the author's request. This separate 12-slide, 5.79 MB draft has 465 seconds of suggested pacing and 1,042 spoken words in its notes. It follows the handwritten chronology: original PbI2 growth/detector problem, ordered Bi2Se3/Bi2Te3 geometry and mosaicity, beam Monte Carlo, decorated Ewald sphere and detector projection, direct measured/calculated comparisons, PbI2 stacking models, and controlled growth. It does not change the manuscript's ordered-film-first priority. The broader 2H/6H/4H history is distinguished from the particular displayed B4 fit, which contains 2H and both handed 6H parents.

The ordered detector pictures were regenerated from the retained September 3 general-CIF SLATE model using 4,096 actual incident states and 4,096 mosaic draws per state. Geometry, source widths, mosaicity, structure, intensity scale and empirical background were frozen. Each measured/calculated pair shares its crop, area averaging and intensity scale. These are new numerical realizations of the retained model, not new fitted structural results. Source arrays and manifests are under `build_codex/slate_talk/ordered_render/{bi2se3,bi2te3}_n4096_m4096/`. Residual elongation differences in Bi2Se3 persist after increasing sampling and are not hidden by display changes.

The author also requested integrating beam position separately to avoid repeating scattering calculations. Implemented this in the external presentation figure renderer, `build_codex/slate_talk/beam_footprint.py` and `render_footprint.py`. For the current homogeneous unbounded flat film with zero external path attenuation, the source-to-sample and sample-to-detector maps give an affine, outgoing-direction-dependent footprint. The implementation conditions the Gaussian position distribution on incident direction, preserving the fitted position–divergence correlation. It retains off-panel mean rays until footprint deposition, preserves detector clipping without renormalization, and guards backward-intersection tails. Scattering weights matched the original calculation exactly for 580 checked on-panel events. Affine intersections agreed within 1.37e-12 native pixels. Direct pixel integrals agreed with an independent bivariate-normal CDF within 4.6e-14 probability mass in the tested cases. A near-singular covariance check also passed.

The final B4 image uses 1,024 angular/wavelength states, 2,048 mosaic draws per state and positive 16-by-16 Gauss-Hermite position quadrature. It is not a uniform blur of an existing image. At 6-by-6 native-pixel bins, doubling the positional order from 8 to 16 changed the image by 0.96% relative L2, the 512-to-1,024 angular-source prefix by 1.48%, and independent mosaic halves by 2.07%. Against a separately generated full-position 4,096-by-4,096 reference, the image difference was 1.54% relative L2 and total diffraction mass differed by 0.158%. The completed position-integrated run took 295 seconds, while the dense reference's accumulation took 1,142 seconds. Different sample counts, worker counts and concurrent load mean this is an observed presentation-render comparison, not a universal or same-work speedup claim. The comparison arrays, probability checks and manifests remain under `build_codex/slate_talk/`. The final B4 display preserves the archived empirical background and fixed magma logarithmic limits of 1 to 106.41679166666533 counts per native pixel.

Slide 8 uses direct September 9 ordered-profile exports, explicitly labelled as a later provisional analysis stage. Slide 11 uses direct saved B4 continuous-profile arrays, not profiles extracted from the new Monte Carlo preview. Both use native editable PowerPoint charts and embedded workbook snapshots. The snapshots use 12 significant digits for portable Excel precision, with maximum absolute rounding below 5e-10 in the supplied values. Full-precision source arrays remain unchanged. All 12 slides were rendered from the exact finalized file and individually reviewed. Package, layout, font, chart/workbook, and import checks passed. Scientific source notes and limitations are retained in the speaker notes. No SLATE production source, saved fit, manuscript figure or advisor evidence gate was changed or closed by this presentation work.

## Presentation Animation: Azimuthal Rotation - 2026-09-10

Created the first requested animation as standalone assets under `output/presentations/animations/sample_rotation_reciprocal_rings`: an 8.4-second H.264 MP4 (1080-by-1080, 30 fps, approximately 0.58 MB), a looping GIF (810-by-810, 20 fps), and a final-frame PNG. The visual follows slide 2's mint flat sample, centered dark normal arrow, and orange in-plane orientation arrow. After an initial pause, the sample makes one counterclockwise turn viewed from above. Off-axis reciprocal points rotate at fixed radial distance and height, leaving traces that become complete circles. The normal arrow and axial 00L points remain fixed. The completed rings are held at the end. This is a schematic construction of in-plane orientational averaging, not a claim that the specimen must rotate during diffraction acquisition. The requested labeled revision is saved separately as `sample_rotation_reciprocal_rings_labeled.mp4` (approximately 0.60 MB), `.gif`, and `_still.png`. It adds upright labels for `HKL rings`, `Sample`, and the exact wording `In-Plane Direction`, with leaders to the rings, sample edge, and rotating orange arrow. The HKL label groups the three schematic rings without assigning unsupported numerical Miller indices. Deterministic geometry and export code are in `build_codex/slate_talk/animate_rotation.mjs`; original and labeled review frames are in `rotation_animation/` and `rotation_animation_labeled/`. Representative frames were visually reviewed, and video/GIF dimensions, frame counts and duration were checked. The existing draft PPTX is unchanged while the animation assets are developed.

The subsequent author-requested revision is `output/presentations/animations/sample_rotation_reciprocal_rings_indexed.mp4` (approximately 0.73 MB), with matching GIF and still PNG. It replaces the in-plane tracking dot and external callout with `In-Plane Direction` printed directly on a wider orange arrow. The text shares the sample-plane rotation and foreshortening. A shared `r = 1` heading and individual `L = 1`, `L = 2`, and `L = 3` labels replace `HKL rings`, ordered from bottom to top; schematic heights are proportional to 1:2:3. These are the author's requested generic schematic indices, not a material-specific reflection assignment. `Sample` and `00L` remain. Review frames in `build_codex/slate_talk/rotation_animation_indexed/` passed independent visual review, and the 8.4-second MP4/GIF dimensions, duration and frame counts were verified. Earlier exports remain available.

## Presentation Animation: Mosaic Tilt - 2026-09-10

Created the requested continuation as `output/presentations/animations/sample_mosaic_caps_and_bands.mp4` (12 seconds, 1080-by-1080, 30 fps, approximately 1.91 MB), with a 720-by-720 looping GIF at 15 fps and a final-frame PNG. Its first source frame is pixel-identical to the preceding indexed-ring animation's final still. A gentle reframe makes room for the tilted crystal normal. The sample, its printed in-plane arrow, the axial points, and the complete r=1 rings share one rigid rotation. A fixed dashed mean normal and faint cone distinguish the reference direction from the moving example. Conical precession first traces a fixed-angle locus; varying tilt magnitudes then introduces smooth purple r=0 spherical caps and teal r=1 spherical bands. The moving example fades and returns to the mean orientation while the accumulated distribution remains. The combined `sample_rotation_then_mosaic.mp4` joins both clips into a 20.4-second sequence.

This is a generic orientation-distribution teaching schematic, not a material-specific reflection assignment or SLATE intensity calculation. The illustrative normal density is Gaussian-like per unit solid angle, with final sigma 4.3 degrees and a smooth cutoff at 12 degrees. These are display choices, not fitted parameters. The ring density uses 256-node azimuthal quadrature of the spherical convolution, projected using front/back ray-sphere intersections on each reflection's constant-|Q| surface. A 768-bin polar lookup avoids visible polygon seams. Each surface's opacity is normalized to its own maximum for shape visibility, so relative reflection intensities and detector line profiles are not claimed. The existing L labels continue to identify crystal reflections rather than fixed laboratory heights after tilting.

Independent geometry checks verified orthogonality, determinant, |Q|, height along the tilted normal and ring radius to within 2e-15. A 256-versus-8192-node quadrature comparison differed by at most 0.113% of the profile peak in the checked initial/final examples. Representative frames and the encoded GIF were visually reviewed; the MP4/GIF dimensions, durations, frame counts and initial-frame continuity were verified. Generator, manifest and review frames are under `build_codex/slate_talk/animate_mosaic.mjs` and `mosaic_animation/`. No manuscript evidence gate was changed by these schematic assets.

## Presentation Animation: Many Crystallites - 2026-09-10

Created the ensemble teaching sequence `output/presentations/animations/many_crystallites_one_pattern.mp4`, with matching GIF and still PNG. The 14-second, 16:9 animation places a fixed substrate and representative crystallites beside one shared reciprocal-space view. A single crystallite expands into twenty initially aligned examples. Independent in-plane angles populate the r=1 rings while r=0 points remain axial. Independent normal tilts then produce the same spherical-cap/band geometry as the preceding mosaic clip. One amber crystallite and its discrete reciprocal points are highlighted together before the final population is held still. Stage captions distinguish the operations with few words. The final H.264 MP4 is 1600-by-900 at 30 fps, 420 frames, approximately 1.25 MB; the looping GIF is 960-by-540 at 15 fps, 210 frames, approximately 2.82 MB. Both durations and frame counts were verified, the complete MP4 decoded without errors, and the encoded GIF was visually reviewed.

Both panels apply the same rigid orientation map. Real-space grain positions are added only to the left-hand geometry and never translate reciprocal coordinates. The twenty illustrative tilts are stratified quantiles of the final per-solid-angle density multiplied by sin(alpha). The continuous density represents the larger orientation population, using the preceding schematic's sigma 4.3 degrees and tapered 12-degree limit. Its intermediate density includes the exact angular pushforward when individual tilts scale toward their final values. Support-limited 256-node azimuthal quadrature and interpolated 768-bin polar profiles avoid undersampling narrow intermediate spreads. The six r=1 points per L and axial r=0 points are generic selected reflection examples, not material-specific selection rules. Coincident display dots are merged, and each continuous surface is normalized separately for shape visibility. No quantitative relative intensity, fitted width or detector pattern is claimed.

Independent review verified the common rotation map, absence of grain-position shifts in reciprocal space, matching orientation laws, and unchanged final orientations. The stratified CDF agrees with an independent finer integration within 5.1e-8 in cumulative probability. Geometry and density helper checks passed, including preservation of reciprocal-vector length, normalization of the scaled orientation law, and narrow-spread convergence. Representative opening, intermediate and final frames were reviewed, with singular opening wording, nonoverlapping caption fades and a distinct amber grain highlight. Generator, density helper and review frames are under `build_codex/slate_talk/animate_ensemble.mjs`, `ensemble_density.mjs` and `ensemble_animation/`. This illustrative animation does not change manuscript evidence gates.

The author-requested clarity revision is saved separately as `output/presentations/animations/many_crystallites_clear_tilt.mp4`, with a matching GIF, final still, and `_tilt_detail.png` teaching still. This 17-second revision pauses after azimuthal rotation ends at 6.7 seconds, dims the orange in-plane arrows, and introduces an enlarged side view before tilting begins at 8.1 seconds. The selected crystallite's finite plate thickness and solid purple normal are shown against its original plate outline and a dashed reference normal. A small alpha arc marks the true tilt, with the same orientation used for the main crystallite and its highlighted reciprocal points. The tilt finishes at 11.8 seconds, the detail is held for two seconds, and the inset fades before the final stationary ensemble.

The additional request to clarify the initial L levels is addressed by lowering the fixed reciprocal-space viewing elevation from 26 to 20 degrees and adding faint gray reference disks at L=1,2,3. Dashed rear edges and solid front edges show perspective, centerlines link each r=0 point to the corresponding r=1 order label, and the axial points also receive individual L labels. These are explicitly labeled reference planes, not continuous scattering from one crystallite. They disappear by 7.7 seconds, before mosaic tilt starts. The density and all reciprocal geometry use the same revised camera. Grain orientations and the final angular distribution remain unchanged from the preceding ensemble animation.

Independent checks found the inset normal's projected angle agrees with its true 5.811118920-degree tilt within 2e-16 radians. The finite plate remains perpendicular to its normal, its reference and tilted outlines coincide at zero tilt, and the final grain orientations exactly match the prior ensemble. Reference-plane heights and their fade timing also pass. Opening, tilt and final proof frames were visually reviewed. The finalized H.264 MP4 is 1600-by-900 at 30 fps, 510 frames, approximately 1.45 MB; the looping GIF is 960-by-540 at 15 fps, 255 frames, approximately 3.31 MB. Both durations are 17 seconds, the complete MP4 decoded without errors, and the encoded GIF's opening and tilt-detail frames were visually checked. The revised generator and proof frames are `build_codex/slate_talk/animate_ensemble_tilt_detail.mjs` and `ensemble_tilt_detail/`; the shared density helper is unchanged.

## Presentation Animation: Ewald Intersections and Mosaic Spread - 2026-09-10

Created a separate 16-second reciprocal-space animation, `output/presentations/animations/ewald_intersections_then_mosaic`, at the author's request. It begins with the three indexed r=1 rings and axial 00L spots, introduces a transparent Ewald sphere, highlights the aligned intersections, then applies normal tilt and conical precession to a representative reciprocal lattice while a continuous mosaic distribution builds spherical caps and bands. The final hold emphasizes their intersections with the Ewald surface. No crystallites or detector are included. Earlier animations and the draft PPTX remain unchanged.

The wavelength, incident direction, sphere and camera stay fixed. In schematic reciprocal units, k=4.2 and ki=(3.9417635647004503,0,-1.45), so the sphere centered at -ki passes through the origin and selects the middle axial order in the aligned state. Each aligned off-axis ring has two isolated intersections. Moving rings use analytic intersection roots. Axial example points are highlighted only at numerical elastic equality, without an invented acceptance width. The accumulated traces lie on exact shell/Ewald intersection circles and sample the same illustrative per-solid-angle distribution as the preceding animations: sigma 4.3 degrees with a tapered 12-degree support. The representative normal reaches 8 degrees and stays within the developing distribution. The fixed camera uses -45-degree yaw and 20-degree elevation. All reciprocal-vector lengths are preserved.

The surrounding density and selected curves share whole-shell profile normalization for each reflection shape. Relative peak strength between reflections, beam broadening, detector intensity and fitted material parameters are not claimed. The final caption is "Mosaic intersections." Geometry checks passed for 918 isolated ring crossings and 1,158 shell-circle points, with maximum Ewald squared-length residual 1.78e-14 and ring-plane/radius errors 1.78e-15 in schematic units. Independent visual review confirmed the labels clear the rotating normal and the final curves remain distinct from the faded density. The generator, geometry helper and proof frames are `build_codex/slate_talk/animate_ewald_mosaic.mjs`, `ewald_mosaic_geometry.mjs`, and `ewald_mosaic_animation/`. The final H.264 MP4 is 1600-by-900 at 30 fps, 480 frames and 687,556 bytes. The looping GIF is 960-by-540 at 15 fps, 240 frames and 2,662,530 bytes. Both durations are 16 seconds. The complete MP4 decoded without errors, every GIF frame was read, and the encoded tilt and final frames were visually checked. Matching aligned, tilt and final PNG stills are also available. This schematic does not change any manuscript evidence gate.

The author's requested ending revision is `output/presentations/animations/ewald_intersections_then_mosaic_clean_end.mp4`, with a matching GIF and still PNGs. Between 13.0 and 13.7 seconds, the stationary ghost rings and axial spots, surrounding caps and bands, and fixed reference normal fade to zero. Only the selected Ewald traces and Ewald construction remain, with the retained reflection labels. The 00L label moves beside the middle purple selected trace and its leader no longer points toward the removed top axial spot. The first 13 seconds reuse the previous decoded video frames, followed by the newly rendered ending, with the full result encoded at the same 1600-by-900, 30-fps specification. The original export remains available. The revised 16-second MP4 contains 480 frames and is 706,063 bytes. Its looping 960-by-540 GIF contains 240 frames at 15 fps and is 2,816,176 bytes. Full MP4 decoding, all GIF-frame reads, and visual checks of the encoded fade and clean final hold passed. Independent review confirmed the original source shapes are absent and the selected curves remain intact. Revised proofs and metadata are under `build_codex/slate_talk/ewald_mosaic_clean_end/`.

## Presentation Animation: Ewald Traces to the Detector - 2026-09-10

Created `output/presentations/animations/ewald_traces_to_detector.mp4` as a separate 16-second continuation, with a looping GIF, final still, and mapping still. The opening source PNG is byte-identical to the preceding clean Ewald ending. The Ewald construction then moves left and a labeled real-space sample point and detector appear on the right. A highlighted reciprocal point defines kf from the sphere center. The matching real-space ray leaves the sample and reaches the detector. Sweeping a teal r=1, L=2 branch draws its detector curve, followed by the other teal branches and the purple axial traces. The construction fades and the camera moves to face the fixed detector, ending on "Projected traces."

The physical map uses kf=ki+q and a unit outgoing direction, followed by an analytic ray-plane intersection. The illustrative detector has normal (0.9385151344524881,0,0.34523809523809523), perpendicular distance 6, vertical center offset 0.075, and half-width/half-height 3.55/3.10 in schematic real-space units. It is not a fitted detector calibration. Both initial panels share the same camera orientation, so corresponding rays also have matching screen angles. Each closed Ewald trace uses 4,096 coordinate samples. Disconnected support components and detector boundaries are retained. The same whole-shell normalized weights control qualitative trace visibility. No counts, detector-density Jacobian, beam broadening, optical response or opaque-substrate acceptance are claimed. A sample point is used consistently with that scope. Static detector strokes use antialiased continuous opacity to avoid seams at adjacent line segments.

Independent checks found interpolated markers remain on the original reciprocal shells and Ewald sphere within 1.78e-15 in schematic units, with detector-plane residual at most 1.56e-15 for the tested markers. The detector texture map agrees with direct real-space projection within 2.28e-13 pixels across the camera movement. The detector coordinates stay fixed. Visual review confirmed corresponding rays, clear labels, smooth traces and the final detector framing. The H.264 MP4 is 1600-by-900 at 30 fps, 480 frames and 714,300 bytes. The looping GIF is 960-by-540 at 15 fps, 240 frames and 3,582,833 bytes. Both durations are 16 seconds. Full MP4 decoding, all GIF-frame reads, and encoded mapping/final-frame inspection passed. The generator, helper and review files are `build_codex/slate_talk/animate_ewald_detector.mjs`, `ewald_detector_geometry.mjs`, and `ewald_detector_animation/`. Earlier animations, the draft PPTX, SLATE fits and manuscript evidence gates remain unchanged.

The author-requested labeled ending is saved separately as `output/presentations/animations/ewald_traces_to_detector_labeled.mp4`, with matching GIF and PNG stills. Eight color-matched r/L labels identify the three axial r=0 traces, both branches of the r=1, L=1 and L=2 features, and the connected r=1, L=3 arc. Short leaders point just before each component's directly projected peak. The faint axial traces retain their original weights. Labels fade in only after the camera reaches its head-on view, from 14.65 to 15.25 seconds, and the final hold is extended to 18 seconds. The earlier motion and all physical geometry are unchanged. Eight pre-ending source proof PNGs are byte-identical to the original proofs.

Independent visual review confirmed correct label assignments and clear text/leader placement. The labeled H.264 MP4 is 1600-by-900 at 30 fps, 540 frames and 782,408 bytes. Its looping GIF is 960-by-540 at 15 fps, 270 frames and 3,696,537 bytes. Both durations are 18 seconds. Complete video decoding, every GIF-frame read, and encoded final-label inspection passed. The existing generator accepts `--labeled` to produce this variant, with proofs and metadata under `build_codex/slate_talk/ewald_detector_labeled_animation/`. The original unlabeled animation and still remain available for the previously created coordinate-morph continuations. Existing PPTX files and manuscript evidence gates are unchanged by this standalone label revision.

## Animated Eight-Minute Presentation - 2026-09-10

Updated the project talk as `output/presentations/Oriented_Powder_8min_Animated.pptx`. This 14-slide, 10.74 MB version has 470 seconds (7:50) of suggested pacing and 1,061 spoken words. Four embedded H.264 videos replace the static orientation and mosaic explanation: indexed azimuthal rotation, single-orientation mosaic tilt, the many-crystallite animation with enlarged tilt detail, and the separate Ewald-intersection/mosaic animation. The Ewald clip used here is `ewald_intersections_then_mosaic.mp4`. The two wide clips have only their constant upper title strip cropped, with editable slide headings replacing that strip. The four videos total 53.4 seconds within the narration budgets. Each is configured to start on the presenter's next click, play once, hold its final state, and permit manual slide advancement.

The sequence retains beam Monte Carlo, the SLATE decorated Ewald sphere and detector projection, measured/simulated detector pairs, the native ordered-film and PbI2 profile charts, and the controlled-growth ending. The original scientific slide XML, four chart parts and four embedded workbook snapshots are preserved byte-for-byte. Beam/projection narration is shortened to fit the new sequence, and one source-note reference now points to the matched-profiles slide. Sources and schematic interpretation limits remain in the notes. The original 12-slide draft is retained.

Verification: the exact final file passes package, layout, font, native-chart/workbook and import checks, and Microsoft OpenXML SDK whole-package validation reports zero errors. All 14 final slide renders were individually reviewed. Slides 8–14 are pixel-identical to the corresponding original slides 6–12. Embedded movie bytes, durations and playback timing were checked. Live PowerPoint slideshow playback was unavailable and remains unverified. Private build assets, validation receipts and renders are under `build_codex/slate_talk/animated_deck/`. No manuscript evidence gate, saved fit, model source or scientific figure was changed by this update.

The author-requested animation update was saved as `output/presentations/Oriented_Powder_8min_Animated_v2.pptx`. It has 15 slides, 480 seconds (8:00) of suggested pacing, and five embedded animations in an 11.65 MB file. Slide 5 uses `ewald_intersections_then_mosaic_clean_end.mp4`; slide 6 immediately continues with `ewald_traces_to_detector.mp4`. Each new clip is 16 seconds. Their constant title strips are cropped and replaced by editable headings, with the scientific content and timing preserved. Beam Monte Carlo follows on slide 7. The retained SLATE decorated-sphere figure is a 15-second bridge on slide 8, keeping the new projection explanation within the talk's time budget. Both clips use the same presenter-next-click, play-once and final-hold settings as the prior version.

The exact v2 file passes package, layout, font, native-chart/workbook, import and whole-package OpenXML SDK checks. All 15 final slide renders were reviewed. The 13 retained slides are pixel-identical to their v1 counterparts; their slide XML, charts, workbook snapshots and retained media also remain byte-identical. The superseded Ewald slide and its exclusive media were removed from the new package, while earlier presentation files remain available. Live PowerPoint slideshow playback remains unverified. Build assets and review receipts are under `build_codex/slate_talk/animated_deck_v2/`. Manuscript evidence gates remain unchanged.

The first termination comparison was saved as `output/presentations/Oriented_Powder_8min_Animated_v3.pptx`: 16 slides, 480 seconds (8:00), five embedded animations, and 11.64 MB. A 40-second termination-sensitivity comparison is inserted as slide 12, after the ordered-film profiles and before PbI2. Its title is "Atomic endings matter between Bragg peaks." Two native editable charts place a nearly invariant r=1 peak beside the strongly changing 003 shoulder. The figure is also exported as `output/presentations/figures/Bi2Te3_Termination_Sensitivity.png`. Earlier narration budgets are shortened; animation durations and playback cues are preserved.

The new figure uses direct archived Bi2Te3 calculations at nominal 5-degree incidence, comparing the original wrapped slab W with an imposed 50:50 intensity mixture of alternative slabs A and B. Composition, interior structure, mosaic, beam, scale and background are fixed. At the selected r=1 peak maximum, the net calculation changes by 0.00315%; all five displayed control bins change by less than 0.15%. The finite-slab boundary factor vanishes at integer crystal-frame ell, while finite detector bins need not be exactly invariant; this is not a claim that every off-axis profile or wing is termination-independent. The horizontal coordinate is explicitly "Nominal L" (the source's |Q|/|c*|), not Qz/|c*|. No smoothing, independent normalization, or reconstructed curves are used. Full-precision exports and provenance are under `build_codex/slate_talk/termination_figure/`; native chart workbooks use 12 significant digits for Excel portability.

This slide demonstrates sensitivity, not a uniquely determined termination population. The later joint termination/mosaic trial remains unaccepted, and its failed comparison guards are not closed by this figure. The supported presentation claim is information beyond bulk Bragg peaks alone. Surface X-ray diffraction already probes termination coverage, so the talk does not claim that conventional techniques cannot determine it. Sources and these interpretation limits are in the slide notes. All 15 previous slide XML parts, five embedded videos, four previous charts and their workbook snapshots are byte-identical to v2; the added slide brings the deck to six native charts and six embedded workbooks. The exact v3 file passes package, layout, font, chart/workbook, import and whole-package OpenXML SDK checks with zero findings. All 16 final slide renders were individually reviewed; the 15 retained slides are pixel-identical to their v2 counterparts. Private build and verification assets are under `build_codex/slate_talk/animated_deck_v3/`. Live PowerPoint slideshow playback remains unverified. No saved fit, model source, manuscript figure or advisor evidence gate is changed by this presentation update.

The parallel termination comparison was saved as `output/presentations/Oriented_Powder_8min_Animated_v4.pptx`, implementing the author's subsequent request to compare Bi2Te3 and Bi2Se3 in parallel. It contains 17 slides, 480 seconds (8:00) of suggested pacing, five animations, and 11.66 MB. The two matching termination slides, logical slides 12 and 13, use the same all-material calculation stage, replacing the older equal-A/B Te example. Both show the 003 core and post-003 wing with identical nominal-L and count axes. Orange is the Bi / central-chalcogen baseline, teal changes only the end-plane mixture at fixed prior mosaic and scale, purple includes the archived joint mosaic/scale refit, and black shows measured values. Beam, background, geometry, composition and interior structure stay fixed. The conditional mixture is 97.55% Bi / outer-Te plus 2.45% Bi / central-Te for Bi2Te3, and 92.81% outer-Se / Bi plus 7.19% Bi / central-Se for Bi2Se3. Names specify top / bottom, with positive c called top. The central/outer distinction refers to position within a quintuple layer.

For a fair descriptive comparison, both slides use core 2.9 <= nominal L < 3.2 and wing 3.2 <= nominal L < 4.5. The plotted points are the actual evaluated bins, without interpolation, smoothing or independent normalization. Error means the unweighted mean squared difference between calculated and measured native-pixel mean counts within that window. Relative to the baseline, endings alone change the Te core MSE by -2.48% and wing MSE by -99.48%; Se changes by +45.55% and -81.42%, respectively. The joint trial changes the Te core/wing MSE by -85.56%/-97.83% and Se by +3.00%/-97.11%. Thus both wings are sensitive to end planes, but the Se termination-only change worsens its core. The joint trial partly compensates for that Se penalty. These shared-window percentages differ slightly from the source task's material-specific masks, which remain archived. The strongest r=1 control bin changes by -0.00110% for Te and -0.01900% for Se from endings alone; weak finite-bin wings need not be invariant.

Direct data source: `C:/Users/Kenpo/.codex/visualizations/2026/09/06/01a0790a-e521-7951-8cf8-97659242b079/all_material_surface_refit.ra_diag.npz`, SHA256 `e8b9b1c5a015a15f65bf1ab2d2e6bad21176c23f775ff0eb64ee1082d60d8b2b`. Full-precision JSON/CSV exports and independent metric reproduction are in `build_codex/slate_talk/termination_comparison/`. Both samples use the archived 256 source states and native q4 detector quadrature, whose source-specific fold convergence remains unqualified. The nominal-L label corrects the source PNG's misleading projected-Qz wording. These are conditional trial weights with no established statistical significance or unique specimen termination assignment. The Te and Se joint trials worsen 13/54 and 39/54 comparison checks, respectively, and remain unaccepted. Se's dominant ending also changed in a later rejected background trial. No model source, saved fit, manuscript figure or advisor evidence gate is changed by this presentation comparison.

The exact final v4 package passes layout, font, package, chart/workbook, import and whole-package OpenXML SDK checks with zero findings. All 17 slides were rendered and individually reviewed. The 15 retained slides are pixel-identical to v3, and all retained slide XML, five videos, timing trees and four previous chart/workbook pairs remain byte-identical. Four new editable charts bring the total to eight, each with an embedded workbook. The obsolete single-termination slide and its exclusive assets were removed from v4. The two new 35-second slides replace one 40-second slide, and five other narration budgets together remove 30 seconds. Animation durations and cues are unchanged. Figure exports are `output/presentations/figures/Bi2Te3_Termination_Comparison.png` and `Bi2Se3_Termination_Comparison.png`. Private finalization and visual receipts are under `build_codex/slate_talk/animated_deck_v4/`. Live PowerPoint slideshow playback remains unverified.

The author also requested a slide reporting the best saved Bi2Se3/Bi2Te3 mosaic and structure-factor parameters. The archive audit found no defensible universal ranking: stages changed objectives, backgrounds, integration regions, numerical operators and orientation measures. The parameter slide therefore reports the best documented retained profile states that exactly match the displayed detector images from `bi2x3-signed-polar-line-refit-2026-09-03`: Se `mosaic_final`, Te `mosaic_pre_caustic`. For Se, Gaussian sigma is 0.9034365805204533 degrees, underlying Lorentz half-width is 0.28187877031599856 degrees, and Lorentz weight is 0.15479764405967122. For Te the corresponding values are 0.39545795076954665 degrees, 0.28810887126398005 degrees, and 0.7448433182525824. Se has the broader Gaussian component, while Te has the larger Lorentz weight. These are not parameters of the later termination trials.

The structure-factor rows are explicitly fixed inputs, not fitted structural discoveries. Se uses zBi=0.4008, zouterSe=0.2117, all-site Uiso=0.019 square angstrom, unity occupancies and 17 conventional-cell repeats. Te uses zBi=0.4000, zouterTe=0.2095, all-site Uiso=0, unity occupancies and 16 repeats. Central-chalcogen z is zero in both CIFs, and optical thickness is separately fixed at 500 angstrom. The zero Te Uiso is an input assumption, not evidence of zero thermal displacement. No termination mixture was refined in these selected stages. Both selected states pass the stored profile/rank/interior gates but fail the width gate, and neither has formal parameter intervals. Median/p95 relative profile L2 errors are 9.7449%/16.8375% for Se and 9.8785%/18.7406% for Te. Another Te stage has a slightly worse median but better p95, so a globally dominant optimum is not established. The library's archival acceptance pointer must not be read as scientific acceptance.

The new mosaic plots show weighted Gaussian and Lorentz contributions and their sum on identical 0–5 degree and 0–2.4 per-degree axes. They use the selected run's wrapped angular-line measure: folded density per degree equals 2*omega(alpha*pi/180)*pi/180, with no spherical sine factor. This is the m0 alpha marginal or absolute rod-local signed-delta marginal, not a complete common SO(3) density. The full-support component areas are 1-eta and eta. The displayed range retains 99.4454% of Se and 97.2724% of Te probability, without cropped-window renormalization. Direct evaluation agrees with the archived function within 5.3e-14, and full-support component integrals match their weights within 1e-12. Source curves contain 1,001 points; native charts retain every fifth point at 0.025-degree spacing and 12 significant digits. Source parameters, hashes, selection audit, full-precision curves and generator are under `build_codex/slate_talk/best_fit_parameters/`. The selected NPZ SHA256 is `10bda11ec46ce258fa603beb22314c72e16fade32884ce259a297ee33d2cc195`. No saved fit, model source, manuscript figure or advisor gate is changed by this parameter report.

The parameter comparison was saved in `output/presentations/Oriented_Powder_8min_Animated_v5.pptx`: 18 slides, 480 seconds (8:00) of suggested pacing, five preserved embedded animations and 11.71 MB. The new 30-second parameter comparison is logical slide 12, after the ordered-film profiles; the parallel termination comparisons are now slides 13 and 14. It contains one editable native table and two editable native mosaic charts, bringing the deck total to ten chart/workbook pairs. Six narration budgets were shortened by five seconds each to preserve the total duration. The slide export is `output/presentations/figures/Bi2Se3_Bi2Te3_Mosaic_Parameters.png`. All 18 exact final slide renders were reviewed. The 17 retained slides are pixel-identical to v4; their slide XML, animation timing, five videos and eight pre-existing chart/workbook pairs remain byte-identical. Final SHA256 is `af274501b2c121df1be146cce3f00115965d539549afeb848ce92ddedafc1061` (11,709,408 bytes). Package, font, native-table/chart/workbook, import and whole-package OpenXML SDK checks passed with zero SDK errors. Private finalization and review receipts are under `build_codex/slate_talk/animated_deck_v5/`. Live PowerPoint slideshow playback remains unverified. The new slide distinguishes fitted mosaic values from fixed structure-factor settings, and its source notes retain the fit-selection, model-measure and uncertainty limitations above.

The final layout receipt's table-overflow warning was reviewed against the actual XML and installed Arial font metrics. Its row estimator ignores the section header's three-column span and incorrectly predicts a wrap for the outer-X label. Every nonempty cell fits its actual usable width. Correcting those two heuristic estimates places the table bottom at 6.4679 inches, with 0.1258 inches before the footnote boundary. The exact final render has no overlap, so no artifact edit was needed. This check does not establish native PowerPoint rendering or playback.

## Presentation Animation Integration - 2026-09-10

The preceding presentation is `output/presentations/Oriented_Powder_8min_Animated_v6.pptx`: 17 main slides with 480 seconds of suggested pacing and 981 spoken words, followed by five hidden backup slides. Ten MP4s are embedded and configured to play once on click and hold their ending. The main sequence retains the ensemble-orientation and clean Ewald-intersection animations, replaces the unlabeled projection with its labeled version, joins the angular-coordinate morph to the Lorentzian-mosaic explanation (30.5 seconds), and joins the Q_R/Q_z morph to general stacking disorder (26.5 seconds). A separate polytype-mixture animation follows. The linked sequences remove duplicated stationary holds while preserving all physical motion at normal speed. The beam Monte Carlo, parallel measured/calculated Bi2Se3 and Bi2Te3 comparisons, mosaic-parameter table and component plots, parallel termination comparisons, and PbI2 fit results remain in the main talk.

Hidden slides 18–22 contain individual in-plane rotation, individual conical mosaic tilt, the static decorated Ewald sphere, the larger-c mosaic example, and the completed Bi2Se3 1–20-degree incidence sweep. The simpler stacking clips keep the diffraction plots large; the newer matrix-panel variants remain standalone. Notes distinguish illustrative spherical-density animations from the retained wrapped angular-line fits, reset the Gaussian schematic before the stacking sequence, and distinguish the illustrative 2H/6H/4H mixture from the subsequent B4 2H/6H+/6H- fit. Incidence-response curves are orientation-intersection weights, not calculated detector counts. No saved fit, model source, manuscript figure or advisor evidence gate is changed.

Final size is 17,714,859 bytes and SHA256 is `0bf99d302cfa229788af9ed0cd6f6bfdf3eb6bf56a03be32ebf1136bbc7086d8`. The final package passes import, font, chart/workbook and package checks, with zero whole-package OpenXML SDK errors, 2,191 preservation checks and ten embedded-video timing checks. All 22 exact final renders were reviewed: the 16 retained slides are pixel-identical to their previously reviewed v5 counterparts, and the six new slides pass individual visual review. All ten native chart/workbook pairs and the native parameter table are preserved. The inherited table-estimator warning on logical slide 10 has the same reviewed false-positive cause described above. Private finalization, media and visual-review receipts are under `build_codex/slate_talk/animated_deck_v6/`. Live PowerPoint slideshow playback remains unverified. The original v5 file is preserved.

### Presentation v7: incidence, stacking matrices and end planes

The preceding presentation is `output/presentations/Oriented_Powder_8min_Animated_v7.pptx`, implementing the author's approved merge/replace/add plan. It has 17 main slides with 480 seconds of suggested pacing and 1,008 spoken words, followed by 13 hidden backups (30 slides total). Ten MP4s remain embedded, configured to play once on click and hold their ending. Slide 6 promotes the updated 24-second Bi2Se3 incidence sweep into the main talk and visibly labels its 50% Lorentzian mosaic as illustrative. The response is an orientation-intersection weight, not a calculated detector count. Slide 14 replaces the previous two PbI2 explanation slides with the combined 24-second transition-matrix, disorder and polytype sequence. The illustrative 50% 2H / 25% 6H / 25% 4H stack population remains distinct from the subsequent B4 2H/6H+/6H- fit. Beam Monte Carlo, the parallel measured/simulated ordered-film images, matched profiles, fitted mosaic components and fixed structure-factor inputs remain in the main talk.

Slides 12 and 13 now open with parallel Bi2Te3 and Bi2Se3 atomic end-plane diagrams. One click fades the introductory diagram in 400 milliseconds to reveal the original editable comparison charts, whose scientific content is preserved. The diagrams show the actual conditional mixtures displayed in those charts: 97.6% Bi/outer-Te plus 2.4% Bi/central-Te, and 92.8% outer-Se/Bi plus 7.2% Bi/central-Se. Top means positive c. Boundary plane order was checked against the source slabs and CIFs; hollow chalcogen symbols identify central sites, not vacancies. The source audit also corrected a private annotation: the Bi2Se3 CIF has intrinsic Uiso = 0.019 square angstrom, while the separately zero-valued radial/normal envelope parameters are different quantities. The existing parameter table already has the correct CIF value. These conditional trials do not establish unique specimen terminations, and the comparison limitations recorded above remain in force.

Hidden slides 18–30 contain the individual rotation and conical tilt, static decorated Ewald sphere, larger-c mosaic example, the standalone 14-second labeled Q_R/Q_z morph, clean 2H/6H/4H matrix-to-atomic-stack figures, both end-plane diagrams, the new Bi2Te3 off-axis and full axial diagnostics, and the paired reflectivity/thickness diagnostic. The three new diagnostic slides preserve source figure pixels, including error bars and negative net counts, and visibly disclose that those source images are not editable. They do not replace the retained main-talk fits. The thickness trial remains unresolved, with 50 nm retained as the optical-thickness input. Backup slides 26 and 27 link to `interactive/surface-population-explorer.html`, a self-contained offline copy that visibly identifies its older equal-A/B trial. Its science arrays and formulas are unchanged; it is distinct from the newer conditional fractions shown in the main slides. `output/presentations/Oriented_Powder_8min_Animated_v7_Portable.zip` packages the exact PPTX and this relative-path HTML companion for transfer.

The final PPTX is 19,124,753 bytes with SHA256 `14372a1f9f7eecc0d6e872ead50f0f1a8ab3744cea44c2e651e07308d64b6b0b`. The exact file passes package, font, chart/workbook and import checks, zero whole-package OpenXML SDK errors, 3,050 preservation checks, ten embedded-video checks and both 400-millisecond fade checks. All 30 exact final slide renders and both revealed chart states have been reviewed: 17 unchanged slides match v6 pixel-for-pixel, 11 authored slides match their individually reviewed renders, and both revealed chart states match v6 exactly. The two opening overlays differ from their reviewed backup counterparts only by the intentionally omitted explorer-link text. All ten native chart/workbook pairs and the native parameter table are preserved. The inherited table-estimator warning is now on logical slide 11 and has the same reviewed false-positive cause described above. Live PowerPoint slideshow playback remains unverified. Private build, source manifests, final renders and validation receipts are under `build_codex/slate_talk/animated_deck_v7/`. Earlier PPTX files, saved fits, model source, manuscript figures and advisor evidence gates are unchanged.

### Presentation v8: regenerated axial reflectivity and restored rotation

The preceding presentation is `output/presentations/Oriented_Powder_8min_Animated_v8.pptx`, following the author's request to regenerate both Bi2Se3/Bi2Te3 detector comparisons with near-origin axial reflectivity and restore the shown in-plane rotation slide before the many-crystallite animation. The existing 8.4-second "In-plane orientations" movie is now main slide 2, immediately before "Many crystallites, one pattern". It is promoted from backup rather than duplicated. The deck has 18 main slides, 480 seconds of suggested pacing and 995 spoken words, followed by 12 hidden backups. The four detector images on slides 9 and 10 are regenerated, and their footers identify the shared capped scale and included low-Qz reflectivity. Other slide layouts, animations, native charts/workbooks and the parameter table are retained.

The new detector pictures preserve the September 3 source distribution, structure, mosaic, count scale and empirical background. Each material uses 4,096 actual incident states and 16,384 native Monte Carlo draws per state for nonzero-r rods. The entire r=0 contribution uses direct detector-area integration of the compatible September 5 local-lamella Parratt-to-kinematic treatment, over the same incident ensemble. This replaces the earlier unstitched axial contribution. It is not added to an already counted r=0 signal. Native forward Monte Carlo explicitly does not support the local-lamella axial stitch, so its guard is retained. The raw detector-valid mask and exact valid-pixel unions are used without a nominal-horizon filter. All five relevant preserved engine modules match the archived optical-model authority. Optical thickness remains 500 angstrom and bottom roughness 10 angstrom. Top roughness is 5.3555911321161505 angstrom for Se and 2.7203872409514145 angstrom for Te. These are conditional optical settings, not a new joint fit or thickness measurement. The later September 10 surface-mixture/thickness trial is a different analysis state and is not pasted onto these maps.

Measured and calculated images use identical 6-by-6 native-pixel area averages, native rows 0–1601 and columns 0–2999, with a shared magma/asinh scale within each pair. The measured 99.9th-percentile cap keeps weak features visible and is disclosed in the slide footer and companion colorbars. Raw numerical arrays remain unclipped. No independent panel normalization or image smoothing is applied. Off-axis split-half L2 differences at this display resolution are 3.95% for Se and 3.55% for Te. Bounded checks of 19 detector blocks per material give 2,048-to-4,096-source L2 changes of 0.21% for Se and 0.97% for Te, with low-angle subsets changing 0.276% and 0.138%. These are regional sampling checks, not a full-image confidence statement. Independent detector-area checks confirm the restored low-angle intensity comes from the axial optical treatment, with background added only once.

Axial quadrature uses increasing order with refinement near sharp features and an omitted-cell audit. Se satisfies the internal local-count tolerance. For Te, five bright-origin cells remain above the internal 0.25% local-count target. Their checkpointed order-128 values are retained: order-64 to order-128 changes no displayed magma RGB channel and changes total axial mass by 0.00703% (axial L2 difference 0.01247%). The explicit presentation-resolution criteria are at most one 8-bit RGB step and less than 0.1% global axial-mass change. This passes the presentation check while retaining `strict_local_tolerance_pass=false` in the Te provenance. The raw counts remain provisional for stricter quantitative use. No higher-order success or publication-grade local convergence is claimed. The limitation is in the slide notes and source receipts, and manuscript evidence gates remain unchanged.

Full numerical arrays, generators, source hashes, optical provenance and independent checks are under `build_codex/slate_talk/animated_deck_v8/ordered_render/` and its parent. Public companion figures are `output/presentations/figures/Bi2Se3_Measured_Simulated_Reflectivity_v8.png` and `Bi2Te3_Measured_Simulated_Reflectivity_v8.png`. The exact final PPTX is 17,758,354 bytes with SHA256 `bb2554f6acc9a27767eff9145382bbab291cf49c4a213fc2b8d7c67a855aca8a`. The portable ZIP includes the exact PPTX and its unchanged relative-path offline surface-mixture explorer. All 30 exact final slide renders and both revealed chart states were reviewed. The 28 visually unchanged slides and both revealed chart states match v7 pixel-for-pixel. The two regenerated comparison slides and their footers pass individual visual review. Package, font, native chart/workbook and import checks pass. The inherited table-estimator warning is now on slide 12 and has the same previously reviewed cause. Live PowerPoint playback remains unverified. Earlier PPTX files, saved fits, production code and manuscript figures are preserved.

### Presentation v9: author-edited deck and parallel Se profiles

The latest presentation is `output/presentations/Oriented_Powder_8min_Animated_v9.pptx`, based on the author's edited Downloads copy of v8, SHA256 `03f7c853bc92153c675364db5a4f9aa055c5c488a8b17a9d6cf98614ae4e8136`. That copy contains 24 visible slides and seven hidden slides. The seven hidden slides and their exclusive package content are deleted entirely. The old slide 12 is replaced by two matching Bi2Se3 slides: off-axis profiles on new slide 12 and the full axial profile on new slide 13. The author's Bi2Te3 pair follows on slides 14 and 15. The other 23 slide designs, author-added detector/specular content, ordering and promoted PbI2 figures are preserved. The final file has 25 visible slides, seven embedded movies, both termination fades, eight native chart/workbook pairs and the native parameter table. Its expanded author-edited sequence is not newly certified as an eight-minute script.

The new Se figures use direct arrays from `C:/Users/Kenpo/.codex/visualizations/2026/09/05/specular_restore/bi2se3_specular.ra_diag.npz`, SHA256 `f70d1d8e6efcbfb55f5db0223a9aacb4644979ae466e7d4c27871ad0c5e29613`. This is one coherent retained September 3 `mosaic_final` structure/source/mosaic state with the compatible September 5 optical restoration, archived background and common intensity scale. Both off-axis sides retain all 75 evaluated bins. The axial plot retains all 290 bins over detector two-theta 1.05 to 29.95 degrees, including low-angle reflectivity. Dark measured-minus-background points and the orange net model use an asinh scale of width 3 without smoothing, clipping, independent normalization or reconstructed curves. Compatible uncertainty estimates are absent from this archive, so error bars are omitted and that absence is disclosed. The selected source state and plot images are identified in the notes. The preserved Te plots remain a later spatial-integration diagnostic stage, and this distinction is explicit.

A bounded archive comparison favored this coherent retained Se state for overall strong off-axis and axial agreement over the later September 9 termination trial. Different masks, backgrounds and objectives prevent a universal historical ranking. The later trial improves the post-003 wing while worsening bright-feature agreement. Neither state resolves the Se 006 discrepancy: the selected curve gives approximately 15 calculated versus 162 measured net counts per pixel area at its measured maximum. The mismatch remains visible and is identified in the notes. No new fit, unique termination assignment or complete refinement claim is made. Final figure sources, exact-array checks and the selection audit are under `build_codex/slate_talk/animated_deck_v9/figures/se_retained/` and its parent. Forty-four independent source-array/hash checks pass.

The exact final PPTX is 13,310,844 bytes, SHA256 `0feca65d8e57c764666012cc75cd880da660fd1fea12eedba0cec69193c2cec3`. All 25 final slides are rendered and reviewed; the 23 retained slide images are pixel-identical to the author's source. The standard package, font, native chart/workbook and import gates pass, and the whole-package OpenXML SDK check has zero errors. The source's themed Calibri text and Arial are preserved. Two inherited layout warnings were reviewed: slide 9 has an oversized text box whose visible text fits, and slide 16 retains the already-audited table-estimator false positive. A source media action's empty relationship identifier is explicitly linked to the same existing movie relationship `rId2`, preserving its `ppaction://media` action, poster and movie bytes. Chart caches are synchronized to their own unchanged embedded workbook cells only where decimal spellings differ but IEEE754 values are identical: 2,709 cached strings in eight charts, with zero binary numerical changes. Workbooks, formulas, chart formatting and animation timing remain unchanged. Final receipts are `final_validation_verified.json`, `final_sdk_validation.json` and `final_visual_review.private.json` in the v9 private build directory. Live PowerPoint playback remains unverified. The uploaded deck, prior decks, saved fits, production model and manuscript evidence gates are unchanged.

## Presentation Animation: Detector to Phi and Two-Theta - 2026-09-10

Created the separate continuation `output/presentations/animations/detector_to_phi_2theta.mp4`, with a looping GIF, final still and intermediate mapping still. The first source PNG is byte-identical to the preceding detector ending. The camera briefly turns to view the unchanged physical detector from the beam side. Three constant-two-theta contours and three constant-phi guides appear, and an amber point identifies one pair of angular coordinates. The detector boundary, guide curves and original traces then morph together into a chart with two-theta on the horizontal axis and increasing phi on the vertical axis. The final plot retains the mapped detector footprint and labels the gray uncovered region "Outside detector."

The angular convention was checked against SLATE-rMC's `src/rasim_next/selection/osc_series.py` and `src/rasim_next/geometry/angles.py`. The source-facing image column is minus the previous detector u direction. The beam-normal row direction is the cross product of the incident unit direction and that column. Two-theta is the angle between the incident and outgoing directions, and phi uses the simulation's wrapped image-azimuth convention. The chosen view places phi zero in the central scattering plane and keeps the entire footprint away from the branch cut. The chart covers 10 to 75 degrees in two-theta and minus 70 to plus 70 degrees in phi. The physical detector covers approximately 13.63694 to 70.85561 degrees and plus/minus 65.95061 degrees respectively.

All eight connected positive-support components are carried directly from the preceding 4,096-sample-per-shell geometry, retaining their qualitative weights and gaps. Each constant-magnitude reciprocal family becomes a vertical segment at its fixed two-theta value. This is a coordinate-mapping schematic, not a rebinned count-density calculation. It does not apply an intensity Jacobian or alter the source, physical detector, reflection weights, saved fits or manuscript evidence gates.

Independent geometry review verified the source-facing sign convention, fixed physical plane, absence of angular wrapping or clipping, and positive local morph Jacobians over the checked detector grid. The forward/inverse map's maximum detector-coordinate error was 9.59e-15 in schematic units. Constant-two-theta trace variation was below 4.3e-14 degrees. Visual review passed for the opening, guide stage, intermediate morph and final chart. The H.264 MP4 is 1600-by-900 at 30 fps, 480 frames and 789,136 bytes. The looping GIF is 960-by-540 at 15 fps, 240 frames and 3,934,929 bytes. Both durations are 16 seconds. Full video decoding, all GIF-frame reads and encoded-frame inspection passed. Generation and review assets are `build_codex/slate_talk/animate_detector_angles.mjs`, `detector_angular_geometry.mjs` and `detector_angles_animation/`. Earlier animations and both existing presentation decks remain unchanged.

## Presentation Animation: Detector to Qz and Radial Q - 2026-09-10

Created the separate continuation `output/presentations/animations/detector_to_qz_qr.mp4`, with a looping GIF, final still and intermediate mapping still. Its first source PNG is byte-identical to the preceding detector ending. A sparse reciprocal-coordinate grid appears over the unchanged detector view. Solid and hollow amber markers identify symmetric detector points. Both detector halves then morph onto the same nonnegative radial-Q chart, and the two markers coincide. The final axes are horizontal Q_R and vertical Q_z, with the mapped detector footprint retained and the gray uncovered region labeled "Outside detector."

The map follows the simulation's nominal sample-frame convention: Q equals K times the outgoing unit direction minus the fixed incident wavevector, Q_z is the component along the mean film normal, and Q_R is the magnitude perpendicular to that normal. The preceding schematic's mean normal is z. Q is not expressed in each tilted crystallite's frame. The signed-side intermediate and public nonnegative radial fold were checked against `ra_sim/simulation/exact_qspace.py`, with matching public axes in SLATE-rMC's retained runtime. SI lines defining Q_parallel, Q_R and Q_z agree. The present fixed-source schematic gives exact mapped Q vectors. A real detector image with beam spread uses the declared nominal projection frame and wavelength, so broadening remains in that chart.

All eight original positive-support components are retained in six reflection display groups, using 4,096 coordinate samples per closed Ewald trace. Each feature remains on its constant-magnitude Q circle after the radial fold. No feature is flattened or pinned to an ideal L height. Coincident display strokes use maximum coverage per reflection to avoid artificial opacity doubling. This is a qualitative coordinate animation, not the simulation's quantitative signal/normalization rebinning operation. Axes explicitly use arbitrary reciprocal units because the original geometry is schematic. The fixed source, detector, saved fits and manuscript evidence gates remain unchanged.

Independent source, geometry and visual reviews passed. The largest checked inverse detector-coordinate error was 4.35e-15 in schematic units. Recovered Q vectors agreed within 2.86e-15 and reciprocal-shell radii within 1.78e-15. The amber markers coincide exactly, both physical half-detectors map to the same radial footprint, and the chart contains the entire detector coverage. The H.264 MP4 is 1600-by-900 at 30 fps, 420 frames and 791,686 bytes. The looping GIF is 960-by-540 at 15 fps, 210 frames and 4,003,044 bytes. Both durations are 14 seconds. Full video decoding, all GIF-frame reads and encoded midpoint/final-frame inspection passed. Generation and review assets are `build_codex/slate_talk/animate_detector_q.mjs`, `detector_q_geometry.mjs` and `detector_q_animation/`. Earlier animations and existing presentation decks remain unchanged.

## Presentation Animation: Labels Through Both Coordinate Morphs - 2026-09-10

The author-requested labeled continuations are `output/presentations/animations/detector_to_phi_2theta_labeled.mp4` and `detector_to_qz_qr_labeled.mp4`, each with a matching GIF, final PNG and mapping PNG. Both begin with the exact source PNG from the labeled detector ending. Color-matched r/L callouts follow the corresponding mapped features, including the faint axial r=0, L=1 and L=3 traces. The angular animation keeps eight upright callouts. During its camera turn, identically indexed symmetric branch anchors switch at the edge-on view, when their projected targets coincide. The Q animation begins with eight callouts and smoothly consolidates the symmetric r=1, L=1 and L=2 pairs into six final reflection labels as the detector halves fold together. The initial grid stage uses the unlabeled detector raster under live labels to avoid stationary duplicate text.

Independent visual review passed for both labeled continuations, including the revised angular midpoint callout path and the Q label merge. Label-box checks over 501 morph states found no overlaps. The angular export is 16 seconds, 1600-by-900 at 30 fps, 480 frames and 1,073,300 bytes. Its looping 960-by-540 GIF has 240 frames at 15 fps and is 4,452,930 bytes. The Q export is 14 seconds at the same video resolution and frame rate, 420 frames and 999,451 bytes. Its matching GIF has 210 frames and is 4,586,799 bytes. Complete MP4 decoding, every GIF-frame read, encoded-frame inspection and exact opening-source checks passed. The existing generators accept `--labeled`; proofs and receipts are under `detector_angles_labeled_animation/` and `detector_q_labeled_animation/` in `build_codex/slate_talk/`. All physical geometry, mapping conventions, reflection weights and support gaps remain unchanged. These remain schematic coordinate animations. Earlier unlabeled outputs, PPTX files, saved fits and manuscript evidence gates are unchanged.

The subsequent author-requested unit revision updates `detector_to_qz_qr_labeled` in place. One displayed reciprocal unit now equals the original schematic r=1 ring radius, R=1.45 in the unchanged native geometry. Both Q_R and Q_z tick values and grid contours use this same scale. Q_R=1 therefore marks the original r=1 radius, and the chosen schematic's ideal L=1,2,3 heights also correspond to Q_z=1,2,3. The footer defines this unit. This schematic choice does not equate normalized Q_R with the discrete reflection-family index generally. The Ewald-selected mosaic maxima retain their actual positions, at Q_R/R approximately 0.99759, 0.98927 and 0.97253 for the three r=1 orders. No feature is snapped to the reference grid. Trace positions, label paths and detector coverage remain unchanged. Independent unit and visual checks passed. The revised 14-second MP4 contains 420 frames and is 953,986 bytes, and the GIF contains 210 frames and is 4,538,342 bytes. Full video decoding, every GIF-frame read, exact opening-source comparison and encoded-final-frame inspection passed. These sizes supersede the preceding labeled-Q export sizes. Matching final and mapping PNGs are updated, while the angular animation, original unlabeled Q animation and PPTX files are unchanged.

The next author-requested angular-animation revision removes the opening camera flip from `detector_to_phi_2theta_labeled` in place. The detector remains at its original head-on width and orientation while the grid appears, then morphs directly into the angular chart. The source-facing parameterization uses symmetry-equivalent branches from the start, so label anchors no longer switch during playback. Each r/L identity, the final angular convention, all trace weights and the final chart remain unchanged. The camera caption and its 1.8-second stage are removed. The grid now appears at 1.4 to 2.3 seconds, marked guides at 2.9 to 3.4 seconds, and the morph at 4.7 to 8.7 seconds, followed by the final hold from 11.3 to 14.2 seconds. Independent visual and mapping review confirmed the fixed opening, continuous labels and consistent shortened timing. The revised MP4 is 1600-by-900 at 30 fps, 426 frames, 14.2 seconds and 974,484 bytes. Its GIF is 960-by-540 at 15 fps, 213 frames, 14.2 seconds and 3,798,312 bytes. Full video decoding, all GIF-frame reads, exact opening-source comparison and encoded-frame inspection passed. These replace the preceding labeled angular export's timing and sizes. Matching PNGs and build receipts are updated. The Q animation, original unlabeled angular animation and PPTX files are unchanged.

## Presentation Animation: Increasing the Lorentzian Mosaic Component - 2026-09-10

Created the separate `output/presentations/animations/lorentzian_mosaic_phi_2theta.mp4`, with matching GIF and Gaussian-only, midpoint and final PNGs. Following the author's correction, this uses the labeled phi-versus-two-theta chart on the left rather than the detector view. Its first source PNG is byte-identical to the latest angular animation's final still. The chart moves left, a crystallite-orientation plot appears on the right, and the Lorentzian probability fraction increases from zero to 65 percent at fixed component widths. The right plot shows the weighted Gaussian, weighted Lorentzian and their sum. All eight r/L callouts remain visible as the tails extend along phi at fixed two-theta.

This remains an illustrative orientation model. It preserves the earlier Gaussian-like density per solid angle, with sigma 4.3 degrees and its smooth 12-degree cutoff. The new component is a full-sphere normalized wrapped Cauchy with illustrative gamma 6 degrees, consistent with the component form in the opt-in spherical law in SLATE-rMC's `src/painted_ewald/normal_density.py` and `mosaic.py`. The mixture is `(1-f)G + fL`, with each component normalized separately over spherical area and total probability fixed. This is not a change to the older flat-latent SLATE APIs or a fitted parameter claim. Directed positive-L parent labels retain their earlier schematic meaning. The exact spherical ring convolution distinguishes the r=1 bands from the r=0 axial caps. The six original constant-|Q| Ewald circles and physical detector acceptance remain unchanged, with 8,192 samples per closed circle and full Lorentzian tails retained.

Each reflection retains its fixed original Gaussian whole-shell peak as the display reference. The left chart settles to the fixed contrast `log(1+30*w)/log(31)` before the mixture starts changing. The right plot shows the per-solid-angle density relative to the original Gaussian peak on a fixed logarithmic scale, over tilt angles zero to 35 degrees. It is a density section, not the polar-angle marginal containing the sine measure. Full-sphere normalization includes angles outside the displayed interval. The initial reframe runs from 0.7 to 2.8 seconds, contrast settles by 3.9 seconds, the fraction increases from 4.8 to 13.8 seconds, and the final state holds to 18 seconds. Central density decreases as probability moves into the broad component. No per-frame peak normalization, radial blur, source change or quantitative count rebinning is used.

Independent geometry and visual reviews passed. Component and ring normalization errors are below 1.9e-14, analytic ring densities agree with independent quadrature within 9.2e-14 relative error, and zero-Lorentzian sample weights exactly match the prior helper. Doubling sampling to 16,384 changes displayed row weights by at most 5.46e-6 and leaves covered half-pixel rows unchanged. The H.264 MP4 is 1600-by-900 at 30 fps, 540 frames, 18 seconds and 871,500 bytes. The looping GIF is 960-by-540 at 15 fps, 270 frames, 18 seconds and 3,442,745 bytes. Full MP4 decoding, every GIF-frame read, exact opening-source comparison and encoded Gaussian/midpoint/final inspection passed. The new generator and helper are `build_codex/slate_talk/animate_lorentzian_mosaic.mjs` and `lorentzian_mosaic_geometry.mjs`; receipts and proofs are under `lorentzian_mosaic_animation/`. Previous animations, PPTX files, model fits and manuscript evidence gates are unchanged. This schematic does not close C18's controlled-comparison requirement.

The author's subsequent readability correction removes all reflection r/L labels and leaders from `lorentzian_mosaic_phi_2theta` throughout the opening, reframe and mixture change. The opening now renders the original unlabeled angular-chart SVG with its two family-legend texts removed, preserving the chart and traces. Axis labels, detector-coverage annotation and the Gaussian/Lorentzian/Sum legend remain. The physical model, fixed scales, mixture schedule and 18-second timing are unchanged. Independent visual review and complete MP4/GIF decoding passed. Updated output sizes are 766,415 bytes for the 540-frame MP4 and 3,025,523 bytes for the 270-frame GIF. Matching Gaussian, midpoint and final PNGs are updated. This supersedes the earlier eight-callout description and labeled opening for this animation only. Earlier standalone animations and PPTX files remain unchanged.

## Presentation Animation: General PbI2 Stacking Disorder - 2026-09-10

Created the separate continuation `output/presentations/animations/pbi2_general_stacking_disorder.mp4`, with a looping GIF, final PNG and midpoint PNG. Its first source frame exactly matches the normalized labeled Q_R/Q_z ending. The original r=0 trace layers and labels remain fixed. The r=1 geometric traces transition to a finite ordered 2H stacking-factor profile, then broaden into continuous diffuse intensity as general stacking disorder increases. Individual r=1 order labels fade into a single diffuse-scattering callout. No 4H or 6H population is added.

The numerical teaching example uses the existing 2H-rich equal-alternative interface law, N=72 layers from the retained renderer, and equal unit trilayer amplitudes to isolate stacking interference. Epsilon increases from zero to 0.8, where all five allowed events have probability 0.2. The final label is "Random allowed stacking" because the forbidden no-shift orientation change still leaves short-range correlations. The terminal rod is therefore smoothly modulated, not perfectly flat or amorphous. Epsilon=1 would exclude the parent event and is not used as a random endpoint. The displayed stacking factor is the exact finite pair sum with correlation eigenvalue 1-1.5 epsilon. Its exported 121-by-4097 profile grid agrees with the actual SLATE recurrence within 9.55e-13, and its integrated unit-period mean stays one within 3.33e-15. This is a stacking-factor schematic in the preceding arbitrary reciprocal metric, not a PbI2 lattice calibration or a full Pb/I form-factor intensity calculation.

Mosaicity, the source geometry and detector coverage remain fixed. The continuous r=1 field uses intrinsic ell=sqrt((Q_R^2+Q_z^2)/R^2-1), rather than identifying intrinsic ell with projected Q_z. It retains the preceding whole-shell mosaic visibility convention and one fixed logarithmic display transfer throughout. All 543,372 supersamples pass exact detector acceptance and mosaic-support checks. Independent sampled mosaic interpolation error was at most 0.000578, and the profile interpolation changed display opacity by at most 0.00642 in 10,000 checks. Independent visual review confirmed the source continuity, unchanged r=0 layers and clear label transitions. The H.264 MP4 is 1600-by-900 at 30 fps, 420 frames, 14 seconds and 345,322 bytes. The GIF is 960-by-540 at 15 fps, 210 frames, 14 seconds and 2,314,900 bytes. Full video decoding, every GIF-frame read and encoded-frame inspection passed. Generation code is `build_codex/slate_talk/animate_pbi2_disorder.mjs`; direct numerical arrays, the export generator, source hashes and review receipts are under `build_codex/slate_talk/pbi2_disorder_animation/`. Existing animations, presentation decks, saved fits, production code and manuscript evidence gates remain unchanged.

The author next requested a larger real-space c lattice parameter to make the axial response clearer during the Lorentzian change. Created the separate `output/presentations/animations/lorentzian_mosaic_phi_2theta_larger_c.mp4`, with matching GIF and Gaussian-only, midpoint and final PNGs. A parameter sweep selected c/c0=1.25, so the reciprocal heights decrease from 1.45L to 1.16L. The in-plane radius, incident beam, physical detector, Gaussian/Lorentzian widths, mixture schedule and display scales are held fixed. The six Ewald shells and their angular positions are recalculated, including the opening frame. The r=0 L=1,2,3 positions are approximately 15.8753, 32.0659 and 48.9487 degrees in two-theta. The r=1 positions are 25.5426, 38.0156 and 53.3347 degrees. All six reflection maxima remain within physical detector coverage. The r=0 cores and tails respond differently: L2/L3 cores decrease as probability moves into the broad component, while broader angular tails become visible. This redistributes axial sensitivity rather than increasing every axial response monotonically.

The r=0,L=1 crossing requires approximately 12.2587 degrees of tilt in this geometry. Its exact Gaussian-only absence is partly caused by the existing 12-degree schematic taper; an untapered Gaussian of the same width would retain a small nonzero value. This feature is therefore not evidence that a Lorentzian is necessary. The animation remains an illustrative parameter variation with no reflection labels, as requested. A footer states that c was increased by 25 percent. Independent geometry, parameter-sweep and visual checks passed, including consistent shifted opening positions and physical clipping. The H.264 MP4 is 1600-by-900 at 30 fps, 540 frames, 18 seconds and 758,466 bytes. The looping GIF is 960-by-540 at 15 fps, 270 frames, 18 seconds and 3,049,451 bytes. Full MP4 decoding, every GIF-frame read and encoded-frame inspection passed. The generator accepts `--c-scale=1.25`; the helper defaults to the original c scale and preserves earlier behavior. Proofs and receipts are under `build_codex/slate_talk/lorentzian_mosaic_larger_c_animation/`. Previous animations, PPTX files, fitted models and manuscript evidence gates are unchanged.

## Presentation Animation: General Disorder to a Polytype Mixture - 2026-09-10

Created `output/presentations/animations/pbi2_disorder_to_polytype_mixture.mp4`, with a looping GIF, final PNG and midpoint PNG. It starts with the exact preceding general-disorder ending and reaches the author-specified population mixture of 50% 2H, 25% 6H and 25% 4H, with zero general-disorder population remaining. The displayed fractions are illustrative population weights, not measured fractions or per-interface fault probabilities. The eight-second change runs from 2.2 to 10.2 seconds, followed by a stationary final hold through 14 seconds. A composition bar and percentage labels show the change. Teal, amber and blue identify the 2H, 6H and 4H contributions.

The calculation retains N=72, equal unit trilayer amplitudes, the arbitrary reciprocal metric, the exact preceding mosaic kernel and detector acceptance, and the original r=0 trace layers. At progress s, linear intensity is (1-s) times the prior general-disorder endpoint plus s times (0.5 S_2H + 0.25 S_6H + 0.25 S_4H). The deterministic parent profiles come from the actual SLATE transition model. The 6H and 4H profiles average symmetry-related handed counterparts, equivalently conjugate rod phases, without asserting fitted handedness. Integer, third-order and half-order features emerge under the same fixed logarithmic display transfer. Color is weighted by each component's linear intensity. The opening profile is copied exactly from the preceding endpoint, and the recomputed initial field is pixel-identical. The r=0, L=3 text moves aside as new bands appear, retaining its original target. Parent callouts point to exact intrinsic-order loci, rather than identifying intrinsic ell with projected Q_z.

Independent numerical and visual review passed. Population weights sum to one, their intensity combination agrees within 1.42e-14, and independent deterministic-stack sums agree with the parent calculations within 2.12e-12. All parent profiles retain unit-period normalization. The final fractions, opening continuity, fixed mosaicity and label placement were verified. The H.264 MP4 is 1600-by-900 at 30 fps, 420 frames, 14 seconds and 478,807 bytes. The GIF is 960-by-540 at 15 fps, 210 frames, 14 seconds and 2,007,893 bytes. Complete MP4 decoding, all GIF-frame reads and encoded-frame inspection passed. The generator is `build_codex/slate_talk/animate_pbi2_polytype_mixture.mjs`; direct profile exports, their generator, source hashes and review receipts are under `build_codex/slate_talk/pbi2_polytype_mixture_animation/`. Earlier animations, PPTX files, production models, saved fits and manuscript evidence gates remain unchanged.

## Presentation Animations: Transition Matrices Alongside Diffraction - 2026-09-10

Rebuilt both PbI2 clips at the author's request as `output/presentations/animations/pbi2_general_stacking_disorder_matrices.mp4` and `pbi2_disorder_to_polytype_mixture_matrices.mp4`, each with a matching looping GIF, final PNG and midpoint PNG. Both videos are 16 seconds, 1600-by-900, 30 fps and 480 frames. The MP4 files are 734,008 and 619,269 bytes. The initial general-disorder frame exactly matches the preceding normalized Q_R/Q_z still. The diffraction plot then shifts right to make room for a labeled six-state transition matrix. Matrix entries change with the same epsilon used for the stacking profile, reaching five allowed probabilities of 0.2 at epsilon=0.8. Rows identify the current layer and columns the next layer, with registry and orientation states defined on screen. One fixed linear white-to-teal probability scale is used throughout both clips.

The mixture clip starts with the exact revised general-disorder ending. Its large matrix shrinks into the General component position before the other fixed component matrices appear. Separate colored population bars and percentages change to General 0%, 2H 50%, 6H 25% and 4H 25%. The 6H and 4H handed variants are shown as pairs with equal weights within each family. Matrix entries and their color scale remain fixed during this population change; no effective averaged transition matrix is presented. The original finite-stack profiles, intrinsic rod coordinates, fixed mosaic kernel, detector acceptance and r=0 trace images are reused. The diffuse and final mixture fields are pixel-identical to the previous versions before the common layout reframe.

The displayed matrices agree with the actual SLATE `full_transition_matrix` and parent laws within 5.56e-17. Row-sum error is at most 2.23e-16, all entries are nonnegative, and forbidden registry-preserving orientation flips remain zero. Independent source, scientific and visual review passed, including the moving layout and both exact frame handoffs. Complete MP4 decoding, all 240 frames of each 960-by-540 GIF, and selected encoded-frame inspection passed. The numerical scope remains the earlier N=72, equal-unit-amplitude stacking-factor schematic in arbitrary reciprocal units; these are illustrative population fractions. Generator and helper are `build_codex/slate_talk/animate_pbi2_matrices.mjs` and `pbi2_matrix_panels.mjs`; proofs and verification receipts are under `pbi2_matrix_animations/`. Previous animations, PPTX files, production models, fitted parameters and manuscript evidence gates are unchanged.

The author then requested joining these stages and removing the repeated matrix zoom/reappearance. Created `output/presentations/animations/pbi2_stacking_disorder_and_polytypes_matrices.mp4`, with a matching GIF and final/midpoint PNGs. This 24-second combined cut reveals the compact matrix layout once, then keeps every matrix and the probability legend at fixed size and position through both stages. The initially evolving population is labeled "Variable stack" until it reaches the general-disorder endpoint. Deterministic parent matrices remain visible with zero population during that first stage. General disorder increases from 4 to 12 seconds, the shared diffuse endpoint holds to 13.5 seconds, populations change from 13.5 to 21.5 seconds, and the final 50/25/25 mixture holds through 24 seconds. The plot reframes only once at the beginning. Numerical profiles and probability laws are unchanged.

Independent review and pixel comparisons confirmed fixed parent matrices throughout, an unchanged general matrix during population mixing, a fixed probability legend, and identical matrix/diffraction regions across the shared endpoint. The original Q_R/Q_z opening is preserved exactly. The 1600-by-900 H.264 export has 720 frames at 30 fps and is 1,174,063 bytes; the 960-by-540 GIF has 360 frames and is 5,084,631 bytes. Complete decoding and selected encoded-frame inspection passed. The combined generator mode is `animate_pbi2_matrices.mjs --mode=combined`, with proofs and receipts in `pbi2_matrix_animations/combined/`. The separate clips remain available and the PPTX files are unchanged.

## Presentation Animation: Bi2Se3 Incidence Sweep - 2026-09-10

Created the separate `output/presentations/animations/bi2se3_incidence_sweep_00l.mp4`, with a looping GIF and first-order, second-order and final PNGs. Following the author's material-specific correction, this uses the conventional hexagonal Bi2Se3 lattice from SLATE-rMC's `examples/bi2se3/structures/Bi2Se3_vesta.cif`: a=b=4.143 angstrom and c=28.636 angstrom, space group R-3m. R centering gives the displayed axial reflections 003, 006 and 009. Their reciprocal lengths are 0.6582468194, 1.3164936389 and 1.9747404583 inverse angstrom. The first in-plane radius is 1.7511941726 inverse angstrom. Faint 101, 104 and 107 bands provide selected orientation context at their physical latitudes; this is not a complete reflection list.

One sphere uses the configured Cu Kalpha1 wavelength, 1.540592925 angstrom, from `configs/bi2se3_simulation.yaml`. Its radius remains 4.0784202012 inverse angstrom. The center moves as minus the incident wavevector, keeping the reciprocal origin on the sphere while incidence increases from 1 to 20 degrees. The camera and mosaic densities stay fixed. Pauses at 4.6287309914, 9.2880213531 and 14.0102957169 degrees show the aligned 003, 006 and 009 conditions. The sphere is shown in an enlarged, cropped view to keep the closely spaced Bi2Se3 caps legible. Only reflection-row labels appear beside the three response plots. Those rows retain the same logarithmic scale and baseline Gaussian reference, with solid mixture curves, dashed Gaussian-only curves, and synchronized incidence cursors.

The preceding illustrative orientation law is retained: Gaussian sigma 4.3 degrees with a tapered 12-degree support, spherical Lorentzian gamma 6 degrees and mixture fraction 0.65. These are not fitted Bi2Se3 mosaic parameters. The plotted observable is the continuous-circle peak orientation density divided by the fixed Gaussian peak. It is not integrated intensity or detector counts. The unchanged Gaussian can already overlap neighboring orders in this lattice. The late sweep demonstrates enhanced off-condition tails, not unique necessity of a Lorentzian. The exact zero in the distant Gaussian response partly reflects the schematic taper. Structure factors, the Kalpha2 line, beam spread, detector acceptance and substrate exit masking are not included. In particular, a reciprocal-space intersection is not a promise of collection above an opaque sample. This animation does not close the C18 evidence gate or modify a fitted model.

Independent CIF, source-hash, reciprocal-basis, extinction, geometry and visual checks passed. The 122,910 checked samples have a maximum Ewald residual below 4.45e-15 and analytic-versus-sampled peak difference below 3.78e-14. The MP4 is 1600-by-900 at 30 fps, 720 frames, 24 seconds and 1,107,122 bytes. The looping GIF is 960-by-540 at 15 fps, 360 frames, 24 seconds and 5,621,126 bytes. Complete video decoding, every GIF-frame read, and encoded opening, three reflection holds and final-frame checks passed. Source PNG versus encoded MP4 mean absolute RGB error is below 0.738 on the 0-to-255 scale. The generator, geometry helper, source-derived response arrays, source hashes and review receipts are under `build_codex/slate_talk/`, in `animate_incidence_mosaic.mjs`, `incidence_mosaic_geometry.mjs` and `incidence_mosaic_animation/`. Earlier animation assets, presentation decks and manuscript figures remain unchanged.

## Presentation Figures: Polytype Matrices and Atomic Stacking - 2026-09-10

Following the author's correction from an animation to three static figures, created `output/presentations/figures/pbi2_matrix_to_stack_2h`, `pbi2_matrix_to_stack_6h`, and `pbi2_matrix_to_stack_4h`, each as a 3200-by-1800 PNG and a fully vector SVG. The 2H figure contains one panel; the 6H and 4H figures compare both handed variants. Each fixed six-state matrix includes numbered outlines on the transitions reached from 0+. Matching numbers identify the state-sequence arrows and the interfaces of a bottom-to-top atomic stack. Every stack shows two complete repeats plus a faded first trilayer of the next repeat, with one repeat bracketed. Matrix fill colors continue to encode probabilities 0 and 1; the colored outlines and circles identify traversal order. The figures distinguish state-orientation signs from the handed-variant signs in parent names.

The projected atomic drawings use the explicit SI registry coordinates and the F+/F− upper/lower iodine definitions, with a shared two-Pb/six-I strip and no ionic charge symbols. Screen x follows basal fractional x and screen height follows the layer normal; basal y is projected out. Lateral and vertical drawing scales are schematic. Registry positions wrap through 0, 1/3, and 2/3 rather than drifting indefinitely. The 4H return step correctly reverses the registry shift after the orientation change. These newly drawn motifs follow the SI equations, rather than reproducing the older TikZ atom placements pixel for pixel.

Independent review verified all five parent panels, all eleven numbered transitions, and all 27 rendered layer states with 216 atomic coordinates. Matrix entries match the actual SLATE parent laws exactly, and exported atomic coordinates match the SI definitions exactly. Repeat brackets enclose one, three, and two trilayers for 2H, 6H, and 4H respectively. The PNG files fully decode at the declared dimensions and are 181,550, 227,719, and 206,801 bytes. The SVG files parse and contain no embedded raster images. All figures were visually reviewed. Generator and helper are `build_codex/slate_talk/make_pbi2_stack_figures.mjs` and `pbi2_atomic_layers.mjs`; manifests and review receipts are under `pbi2_stack_order_figures/`. This request produced static figures only. Previous animations, PPTX files, manuscript figures, production models, fitted values and evidence gates remain unchanged.

## Presentation Animation: Bi2Se3 Incidence Angular Image - 2026-09-10

The author's readability revision is `output/presentations/animations/bi2se3_incidence_phi_2theta.mp4`, with a looping GIF and 003, 006, 009 and final PNGs. A synchronized amber core, expanding ring and temporary reflection label mark each exact cap-center crossing on the Ewald sphere and at the corresponding phi=0 position in the angular image. An amber outgoing radius briefly connects the sphere center to that cap center. Events begin at 4.0, 10.8 and 17.0 seconds, at the same three Bi2Se3 aligned incidences as above. Each cue lasts 1.45 seconds and disappears before the incident-angle sweep resumes. These are annotation overlays, not changes to diffraction weight or an invented angular acceptance tolerance.

The three peak-weight comparison plots are replaced by a live phi-versus-two-theta image covering two-theta 5 to 36 degrees and phi -90 to 90 degrees. Its beam-centric coordinates match the SLATE angular convention: two-theta is measured from the current incident direction and phi=0 lies toward the projected mean normal. The image evaluates the same fixed Gaussian/Lorentzian mixture at the exact shell/Ewald point for every half-pixel phi row. Purple shows the three axial reflections; thin teal context shows 101, 104 and 107. The 104/009 separation of 0.23047 degrees is preserved. Their display strokes use 1.5 and 3.2 pixels respectively to retain separate identities at this image size. Stroke width is a drawing convention, not a fitted radial width. Axes, contrast, lattice, wavelength and mosaic parameters are fixed through the sweep. This remains an illustrative angular projection without structure-factor weighting, detector acceptance, substrate exit masking or detector-count normalization. The original peak-weight animation remains available.

Independent angular-coordinate and image-weight checks passed. The Ewald residual is below 8.5e-15, coordinate roundtrip error below 1.8e-15 radians, agreement with the prior SLATE-style angle convention within 1.1e-13 degrees, and the context-density lookup error below 5.9e-6 absolute mixture weight. Exact north-pole events pass; near misses at 1e-8 radians are rejected. All flash overlays were checked against the exact crossing geometry. Visual review passed, including the close 104/009 pair. The 24-second H.264 MP4 is 1600-by-900, 30 fps, 720 frames and 984,989 bytes. The 960-by-540 looping GIF is 15 fps, 360 frames and 5,205,495 bytes. Complete decoding, every GIF-frame read, encoded-frame comparisons and paired-flash visibility before, during and after all three events passed for both formats. Generation and review assets are `build_codex/slate_talk/animate_incidence_angular.mjs`, `incidence_angular_geometry.mjs` and `incidence_angular_animation/`. Presentation decks, prior animations, fitted models and manuscript evidence gates are unchanged.

The author's subsequent c-lattice consistency request prompted an independent audit of both panels. The previous 101, 104 and 107 coordinates already used c=28.636 angstrom correctly; the omissions were the interleaved 012, 015 and 018 bands and all off-axis selected Ewald traces in the left panel. The revised same-name MP4, GIF and stills now include all six positive-L first-basal-radius bands through L=9, together with 003, 006 and 009. Both panels obtain the selected points from the same physical angular geometry. Every mean band uses Q_z=2*pi*l/c and Q_r=4*pi/(sqrt(3)*a). R centering is respected: 10l has l=1,4,7 and 01l has l=2,5,8. Teal Ewald intersections now connect the bands visually to their angular-image streaks. The timing, three exact axial flashes, display axes, lattice, wavelength and illustrative mosaic parameters retain their preceding values.

The independent audit parses the actual CIF, beam configuration and VESTA reference table. The nine displayed labels exactly match that reference's positive-L reflections in the 5-to-36-degree window. Reciprocal coordinates agree within 4.5e-16 inverse angstrom, and the 178,101 sampled points across incidence 1 to 20 degrees have maximum Ewald residual 1.12e-14. Reference d spacings and two-theta positions agree within the printed precision. The updated 24-second MP4 is 1,265,063 bytes and the GIF is 6,314,995 bytes. Complete decoding, all GIF-frame reads, source-versus-encoded frame checks and paired flash visibility checks pass; the maximum selected-frame mean RGB error is below 0.918 on the 0-to-255 scale. Audit and media receipts are in `build_codex/slate_talk/incidence_angular_animation/`, including `bi2se3_first_family_verification.json` and `media_verification.json`. These remain teaching visuals, without structure-factor weighting or quantitative intensity validation; no manuscript evidence gate is closed.

The subsequent clarification revision explains the unnumbered filled cells directly below each matrix. The 6H panels identify the opposite-trilayer-orientation cycle and print its complete minus-state path. The 4H panels identify equivalent cycles from different starting registries and print both remaining two-state cycles. The 2H panel explains the other same-state repeats. A shared footer now states that the numbers trace the example starting at 0+ and every filled cell has probability one. The existing state-orientation versus parent-handedness key remains. These additions leave the numbered paths, matrices, atomic stacks and repeat brackets unchanged. Verification additionally confirms that the displayed unnumbered cycles account for every remaining state and that the 4H alternatives are registry translations of the numbered cycle. Independent scientific and visual review passed. Updated PNG sizes are 204,399 bytes (2H), 248,628 bytes (6H), and 230,849 bytes (4H); the corresponding vector SVGs are updated at the same paths.

## Presentation Animation: Reduced Lorentzian Contribution - 2026-09-10

At the author's request, remade `output/presentations/animations/bi2se3_incidence_phi_2theta.mp4`, its GIF and four stills with the illustrative Lorentzian fraction reduced from 0.65 to 0.50. Gaussian sigma=4.3 degrees and spherical Lorentzian gamma=6 degrees are unchanged, as are the nine Bi2Se3 reflections, lattice, incidence trajectory, fixed selected-trace/angular-image contrast and three exact alignment flashes. Every active cap, band and angular-profile path uses the same fraction. Independent checks confirm pure far-tail weights decrease by 0.50/0.65=0.7692307692 at the fixed Gaussian reference. The existing static-background cap-peak display normalization is retained. These parameters remain illustrative, not fitted values.

Geometry and mixture checks, representative-frame visual inspection, complete MP4 decoding, every GIF-frame read, and all paired-flash checks passed. The 24-second, 1600-by-900 MP4 has 720 frames and is 1,255,965 bytes; the 960-by-540 GIF has 360 frames and is 6,479,195 bytes. The refreshed animation manifest and media verification receipt are in `build_codex/slate_talk/incidence_angular_animation/`. A previous 0.65 manifest and representative frame are retained there in `before_fraction_050/`. Presentation decks and fitted models were not changed.

The author's next trial reduces the Lorentzian fraction to 0.25, with all component widths, lattice geometry, reflection positions, display rules and timing retained. The canonical animation and stills are refreshed; the verified video and GIF are also saved as `output/presentations/animations/bi2se3_incidence_phi_2theta_lorentzian_25.mp4` and `.gif`. The preceding 0.50 versions are retained at the corresponding `_lorentzian_50` paths for comparison. Visual review confirms weaker tails with readable caps and paired flashes. Geometry, mixture and full media checks pass, including every flash and all GIF frames. The 24-second MP4 is 1,225,005 bytes and the GIF is 6,572,358 bytes. The preceding manifest and representative frame are preserved in `incidence_angular_animation/before_fraction_025/`; current manifests and verification receipts describe the 0.25 trial. This is an illustrative mixture change only.

## Presentation Figures: Separate Polytype Captions - 2026-09-10

At the author's request to reduce floating text, created separate clean versions of the three static figures as `output/presentations/figures/pbi2_matrix_to_stack_{2h,6h,4h}_clean.png` and matching SVGs. Removed explanatory subtitles, unnumbered-cycle notes and footers from the images. Essential state labels, numbered matrix/sequence/interface correspondence, repeat brackets and compact legends remain. The 6H and 4H matrices are enlarged. `output/presentations/figures/pbi2_matrix_to_stack_captions.md` provides a short caption per family and a shared key suitable for presentation notes. These retain the explanations of unnumbered 6H opposite-orientation cycles, 4H registry-translated cycles, other 2H starting states, and the independent meanings of state and parent signs.

All three 3200-by-1800 PNGs and fully vector SVGs passed visual review. Independent caption and visual review passed. The numerical verifier confirms exact agreement with the production parent matrices and SI atomic coordinates, including numbered paths and coverage of all unnumbered states. The clean PNG sizes are 131,927 bytes (2H), 183,372 bytes (6H), and 160,276 bytes (4H). The generator's `--clean` mode and receipts under `build_codex/slate_talk/pbi2_stack_order_figures/clean/` preserve the prior annotated outputs. This revision changes presentation figure layout and captions only.

## Presentation Animation: Verified Reflection Positions and r=3 - 2026-09-10

The author requested verification of the r=1 locations and addition of r=3. The existing r=1 sharp-reflection coordinates were correct. The new `build_codex/slate_talk/bi2se3_animation_lattice.mjs` reads lattice lengths from the actual CIF linked by SLATE's Bi2Se3 simulation configuration and reads the retained Cu Kalpha1 wavelength from that same configuration. It derives the reflection coordinates and applies the source CIF's R-centering rule before rendering. The family definition follows manuscript Eq. `eq:hexagonal_family_index`: r=h^2+hk+k^2 and Q_R proportional to sqrt(r). The selected r=3 reflections are 110, 113, 116 and 119, with common Q_R=3.0331572808 inverse angstrom and two-theta values 43.6601, 44.7306, 47.8286 and 52.6828 degrees. Each off-axis band uses its own radius in its polar latitude and density calculation.

Created `output/presentations/animations/bi2se3_incidence_phi_2theta_r0_r1_r3.mp4`, matching GIF and a 009-crossing still, and refreshed the canonical same-name incidence assets. Purple, teal and rose identify r=0,1,3. The 13 selected reflections have 0<=L<=9; the animation is not a complete diffraction pattern in the expanded window. The two-theta range is now 5 to 56 degrees and phi is -110 to 110 degrees, retaining the 110 sharp branches that reach +/-98.38 degrees at 20-degree incidence. A small family legend replaces any need for persistent individual peak labels. The camera is reframed to include the larger rings. Thin fixed drawing strokes retain the close 104/009 separation. The 25% Lorentzian fraction, component widths, 24-second timing and the original three paired axial flashes remain.

Independent verification matches all 13 selected reflections to the source CIF and VESTA table. All 257,257 sampled shell/Ewald points and 1,540 independent analytic positive/negative-phi intersections pass; the latter agree within 1.54e-15 inverse angstrom. The Gaussian and Lorentzian band calculations were checked separately at the physical radii. Visual inspection, full MP4 decoding, every GIF frame, source/encoded frame comparisons and all three paired flashes pass. The MP4 is 1,352,715 bytes and the GIF is 6,315,556 bytes. Current source hashes, geometry reports and media receipts remain under `build_codex/slate_talk/incidence_angular_animation/`.

Scope clarification from this audit: the smooth mosaic bands use uniform independent in-plane spin. Current SLATE's tied rotations of individual rods do not produce exactly that same broadened marginal. The displayed Bragg geometry is material-specific and verified; the mosaic line shapes and relative brightness remain illustrative, with no structure-factor weighting or detector/sample-exit mask. In particular, the ideal 110 ring has q_z=0 and its outgoing ray is below the sample plane at positive incidence. Its inclusion represents full angular geometry, not a claim of collection above an opaque sample. This work does not establish full SLATE image parity or close any manuscript evidence gate. Prior named 25% and 50% exports and presentation decks remain available.

## Status Definitions

| Status | Meaning |
|---|---|
| TODO | Not started or not yet placed into the manuscript. |
| IN PROGRESS | Work exists, but it is incomplete, unclear, or not manuscript-ready. |
| DONE | Incorporated into the manuscript or supplement in a form ready for advisor review. |
| PARKED | Important, but intentionally deferred. |
| CHECK | Needs advisor verification or a decision. |
| BLOCKED | Cannot be completed until a specific missing input, source, or calculation is available. |

## Advisor Comment Audit And Immediate Revision - 2026-08-06

The current annotated manuscript contains 12 substantive comments. The immediate revision follows the advisor's established division of labor: the main text teaches the physical sequence and the observable consequence, while coordinate bookkeeping, ray-plane intersections, optical boundary equations, numerical quadrature, and full profile definitions are placed in the Supporting Information. Grazing-incidence optics is treated as a standard correction, not as a principal contribution.

| ID | Advisor comment | Status | Implemented response and remaining gate |
|---|---|---|---|
| A01 | Discuss receiving detector geometry as clearly as sample geometry. | DONE | Section 2.1 now explains sample acceptance and detector acceptance as two consecutive physical operations. SI Sec. S1 gives the sample-plane and tilted-detector-plane intersections, continuous pixel coordinates, front-face condition, active-area bounds, and calculation order. The ordered-film configuration is correctly described as an unbounded in-plane sample plane, so no unsupported sample-edge broadening is claimed. |
| A02 | Use the conventional crystallographic definition of displacement parameters. | DONE | The main text now writes the structure factor with the conventional crystallographic displacement factor `T_a(hkl)`. SI Sec. S2 defines the isotropic U/B relation and records the current implementation scope: isotropic CIF displacement input and one shared uniaxial film-frame pair when directional damping is released. General site-resolved anisotropic CIF tensors are not claimed. |
| A03 | Remove the set-builder equation for the r family. | DONE | The set equation and member-count symbol were removed. Prose now states that common r means common ideal Q_R, while every allowed physical (h,k,l) member is evaluated before summation and no second multiplicity factor is applied. |
| A04 | Explain the relative 003/006 intensity and the extended 003 streak. | DONE | Section 2.3 and Fig. 5 now separate the radial $2\theta$ extension from the transverse $\phi$ width. The successful full-spectrum versus mean-wavelength control holds geometry, structure factors, mosaic density, background treatment, and detector mapping fixed. The full spectrum reproduces the long radial $003$ streak and enhanced apparent $003$ signal, while the mean-wavelength control collapses the streak and retains the transverse mosaic-sensitive width. The text explains that $006$ lies at approximately twice the radial coordinate of $003$ and that the observed contrast also reflects the stronger low-$Q_z$ radial intensity envelope sampled near $003$, not the structure factors alone. SI Sec. S3 records the controlled comparison. |
| A05 | Make Figure 6 carry the two-component mosaic explanation. | DONE | Figure 6 now labels the narrow core and broad low-probability component and explains which detector features sample each angular range. The caption explicitly states that the schematic explains sensitivity but is not an ablation. The no-tail and reoptimized Gaussian-only tests remain the separate C18 evidence gate. |
| A06 | Explain wavelength sampling as part of the overall calculation strategy. | DONE | Section 2 begins with the complete physical sequence. Section 2.4 now introduces position, direction, and wavelength as coordinates of one weighted incident state. SI gives the equal-mass antithetic Latin-hypercube source construction and convergence requirements. The continuous mosaic density and analytic Ewald roots are distinguished from a stochastic crystallite cloud. |
| A07 | Decide whether grazing-incidence optics belongs in the main text. | DONE | The optical derivation was removed from the main text. One short paragraph states that the standard correction is strongest near critical-angle conditions and is not a principal result. Boundary equations, branch conventions, attenuation, flux normalization, and wavelength dependence remain in SI. No separate optics sensitivity or optics-off test is included in the present manuscript. |
| A08 | Define or remove the optical-to-kinematic phrase and account for the reflectivity work. | IN PROGRESS | The unclear phrase was replaced by "Parratt-to-kinematic crossover," defined as a smooth connection between the low-$Q_z$ Parratt limit and higher-$Q_z$ finite-stack kinematic response. A new main-text figure slot now reserves a joint low-$Q_z$ through $003$ comparison using one coordinate transformation, background treatment, and intensity scale, with an expanded full-spectrum versus mean-wavelength $003$ panel. The final direct-array reflectivity panel remains to be inserted, so the comment is not yet fully closed. |
| A09 | Rewrite the held-out-prediction language. | DONE | Machine-learning terminology was removed. The text now states plainly that the 5, 10, and 15 degree datasets entered the staged refinement and that the displayed 5 degree condition is a fitted comparison, not an independent incidence-angle prediction. |
| A10 | Specify where the ordered-film comparison is shown. | DONE | The introduction and Section 3 now identify the Bi2Se3 and Bi2Te3 comparison figures explicitly. |
| A11 | Define the extraction or projection operation. | DONE | Section 2.1 and Section 3 state that each calculated trajectory defines a finite integration region and coordinate. Measured signal and calculated detector density use the same calibrated map, limits, and area normalization, with signal and normalization accumulated before division. SI gives the finite detector and angular-bin definitions and uncertainty propagation. |
| A12 | Make Figure 7 readable and explain how the profiles were generated. | DONE | The former composite was split into one full page per material. Each page now has large descriptive headings, a large detector panel, a larger profile panel, and a teaching caption. The caption defines centerlines, integration regions, detector-side signs, measured/calculated curves, and the supported claim. This layout correction does not resolve the separate provenance and common-scale limitations of the retained historical plot assets. |

Immediate manuscript work is complete for A01--A07 and A09--A12. A08 is in progress: the terminology, section logic, and joint figure location are resolved, while the final direct-array reflectivity panel remains to be inserted. The broad-component necessity test under C18 and direct ordered-figure provenance remain independent evidence gates rather than prose tasks.

## Advisor Comment Audit And Revision Tasklist - 2026-07-18

The packet inventory was checked against the annotated PDF rather than inferred from the response-plan Markdown. The 29-page PDF contains exactly 22 substantive annotations by Paul Miceli: 18 sticky notes and 4 highlighted-text comments on pages 1, 3, 5, 6, 7, 8, 11, 14, and 17. No substantive annotation is missing from the response plan.

Audit verdicts:

- **AGREE**: the packet's proposed response is appropriate as written in substance.
- **AGREE WITH MODIFICATION**: the advisor's intent is sound, but the proposed implementation is too broad, too certain, duplicative, or must be conditioned on the actual model/data.
- **DISAGREE WITH PROPOSED IMPLEMENTATION**: the underlying advisor concern is valid, but the packet prescribes wording or work that should be replaced rather than merely adjusted.
- **CHECK**: the advisor's wording is ambiguous enough that it should not be treated as a settled literal instruction.

Overall, the 2026-07 packet was directionally strong. This paragraph records the historical closure state from that pass. The later 2026-08-06 audit obtained the executable model and configuration, closed the geometry and calculation-order parity items, corrected the source-versus-mosaic sampling description, and demoted optics in the main text. The remaining scientific evidence gates are the controlled mosaic ablations, direct ordered-figure provenance, and the final direct-array near-critical reflectivity panel.

One packet instruction is stale relative to the repository: the ordered Bi2Se3/Bi2Te3 detector and profile panels no longer need to be “restored.” The assets exist and already show close measured/calculated overlays. They still need readability checks, residual/uncertainty support, and verification that the displayed curves and labels are generated from the final analysis.

### Independent critical findings beyond the packet

1. **The former inverse-order bandwidth argument has been removed and replaced by a direct control.** The main text and SI reconstruct the Ewald sphere for every wavelength and incident-direction state. The successful full-spectrum versus mean-wavelength calculation reproduces and then collapses the radial $003$ streak under otherwise fixed conditions, assigning that radial component primarily to wavelength dispersion.
2. **The incorrect hard-coded 003/006 angles have been removed.** The active text states only the robust relation that $006$ lies at approximately twice the radial $2\theta$ coordinate of $003$. The full detector calculation, rather than subtraction of incidence angle from a displayed detector angle, is used to interpret their visibility and the radial streak.
3. **The optical equations and executable boundary implementation are cross-checked.** The main text and SI conserve tangential wavevector, use the decaying complex-$k_z$ branch, integrate attenuation over normal depth, and distinguish field amplitudes from normal flux. Optics is a standard SI correction rather than a manuscript contribution. No separate optical sensitivity test is part of the present revision.
4. **The near-critical connection is now named and bounded clearly.** The manuscript uses "Parratt-to-kinematic crossover" for the smooth connection between the low-$Q_z$ Parratt and higher-$Q_z$ finite-stack kinematic limits. A joint low-$Q_z$ through $003$ figure slot is present, but its final direct-array panel is still required before the reflectivity comparison is complete.
5. **The ordered comparisons are now described in plain experimental language.** All three incident angles are disclosed as refinement inputs. The section heading and prose avoid held-out, in-sample, and validation language and state that the displayed condition was included in the refinement.
6. **The structure-factor convention now follows crystallographic usage.** The main text uses fractional coordinates and a conventional displacement factor \(T_a(hkl)\). The SI gives the isotropic U/B conversion and the verified current implementation scope. General site-resolved anisotropic CIF tensors are not imported, and the manuscript no longer implies otherwise.
7. **The next-draft PbI2 scope is disproportionate.** `results_diffuse_pbi2.tex` is 348 lines versus 49 lines for the ordered test cases and still reports a pending transition-matrix refit. For the next advisor draft, either omit PbI2 from the main text or reduce it to the measured motivation and one final data/model panel; move the transition-matrix derivation, parameter table, and parent-stack detail to SI.
8. **Notation and supplement sources are consolidated.** Built sources now use \(Q_R\), lower-case Miller indices, \(p_{\mathrm{tail}}\) for the mosaic mixture, distinct phase/index symbols, and explicit family-versus-multiplicity language. The standalone implementation outline was merged into the built SI and deleted rather than retained as a competing source.
9. **The ordered overlays are more readable but not yet provenance-ready.** The two materials now have separate full-page figures with larger detector and profile panels and descriptive headings. The retained assets still lack complete direct plotting arrays and a current accepted generation record. Record normalization and scaling, verify whether relative intensities share a meaningful scale, retain direct source arrays/scripts, and add residual or uncertainty support before using them for stronger quantitative claims.
10. **The ambiguous “grown by TEM” phrase was removed.** No replacement growth method was invented; a specific method can be added later only from verified experimental provenance.

| ID | Advisor request | Audit verdict | Current state | Priority | Independent recommendation and acceptance gate |
|---|---|---|---|---|---|
| C01 | Open with 2D van der Waals films | AGREE | DONE | P1 | Resolved in the opening paragraphs and first figure of `sections/introduction.tex`. The 115-word first paragraph now follows layered-film importance -> substrate/growth-dependent azimuthal registry -> operational 2D-powder definition -> simultaneous area-detector observables, with no model or software language. The material-family catalogue was removed, the orientational-limits comparison is now the first visual, citation fit was checked, and a clean build plus rendered review of `main.pdf` pages 1-3 verified the change. |
| C02 | Explain the Ewald sphere and diffraction selection | AGREE WITH MODIFICATION | DONE | P1 | Resolved in Fig. 1 and its explanatory paragraph in `sections/introduction.tex`. The caption and prose connect isolated point, spherical-shell, and axial-ring intersections to sparse spots, Debye--Scherrer rings, and paired 2D-powder features. The 2026-07-19 author-proof pass restored the earlier eight-panel layout and curated reciprocal-space panels while retaining that physics explanation. The obsolete single-crystal detector schematic and the redraw script that generated the rejected replacements were removed. The clean isolated build and rendered page 2 passed. |
| C03 | State accomplishments and outline the paper | AGREE | DONE | P2 | The close of `sections/introduction.tex` now states only the displayed in-sample fit result and gives the final roadmap: ordered test cases, workflow, then PbI2 extension. It makes no relative-intensity, held-out-prediction, or unique-tail claim. The guarded build has no unresolved references or citations. |
| C04 | Define \(\mathbf k_i\), \(\mathbf k_f\), \(\mathbf Q\), and \(\mathbf G\); number equations | AGREE | DONE | P1 | Resolved at the opening of `sections/modelling_methods.tex`: \(\mathbf k_i\) is the incident wavevector, \(\mathbf k_f\) the scattered wavevector, \(\mathbf Q\) their difference, and \(\mathbf G\) the reciprocal-lattice vector for \((h,k,l)\). Elastic scattering, momentum transfer, Bragg matching, and the scattered-wavevector relation are numbered and referenced as Eqs. 1--4. C04 itself introduced no frame superscripts or rotation-matrix rewrite; separate geometry-interface work for C05--C08, C10, and C11 supplies the broader SI treatment. The final main build, reference scan, and rendered Methods page verify the change. |
| C05 | Define sample intersection | AGREE WITH MODIFICATION | DONE | P1 | Section 2.1 explains sample acceptance physically and SI Sec. S1 gives the forward ray-plane intersection and optional finite support. The implementation/configuration audit confirms that the displayed ordered-film calculations use an unbounded in-plane sample plane, so sample-edge clipping is not claimed as a fitted broadening mechanism. |
| C06 | Unpack incidence geometry and optical corrections | AGREE WITH MODIFICATION | DONE | P1 | The Methods separates sample intersection, local incidence, standard planar-interface correction, scattering restriction, external exit direction, detector mapping, and finite integration. SI records the verified operation order. The optical mathematics is kept subordinate in SI. |
| C07 | Introduce Monte Carlo sampling with incident rays | AGREE WITH MODIFICATION | DONE | P1 | The implementation audit corrected the earlier description. The incident source is represented by seeded equal-mass antithetic Latin-hypercube states in position, direction, and wavelength. The crystallite-orientation distribution is a continuous density and its Ewald intersections are solved analytically rather than drawn as a second Monte Carlo cloud. |
| C08 | Explain footprint from sample intersection | AGREE WITH MODIFICATION | DONE | P1 | The footprint is defined as the weighted distribution of accepted sample-plane intersections, not a second displacement. Methods, SI, Figure 4, and the audited implementation use this same meaning. Optional finite support is documented without claiming edge clipping for the ordered-film results. |
| C09 | Replace vague labels/profile extraction language | AGREE WITH MODIFICATION | DONE | P1 | `sections/modelling_methods.tex` and the caption in `sections/results_ordered.tex` now identify physical \((h,k,l)\)/\(r\) labels, trajectory centerlines, finite detector integration bands, and matched measured/calculated \(Q_z\) or \(L\) profiles using \(Q_R\) consistently. The final main build and static notation scan verify the change. |
| C10 | Explain reciprocal-space construction algorithm | AGREE WITH MODIFICATION | DONE | P1 | SI defines physical rod identities, systematic absences, detector-coverage bounds, weighted source states, continuous mosaic density, analytic moving-Ewald intersections, detector acceptance, and convergence. The description now matches the audited production calculation rather than an event-deposition algorithm. |
| C11 | Put geometric-correction equations in SI | AGREE | DONE | P1 | SI Sec. S1 now contains sample-plane acceptance, the detector rigid transform, tilted detector-plane intersection, continuous pixel coordinates, front-face and active-area tests, and the finite-region integration definition. These equations were checked against the available executable model and result-measure specification. |
| C12 | Repair Figure 4 and caption | AGREE | DONE | P1 | `figures/geometry/sample_geometry_rotation.tex`, `figures/geometry/sample_geometry_alignment.tex`, and the caption in `sections/modelling_methods.tex` now show and define \(\mathbf r_{\mathrm{hit}}\), remove the undrawn \(\delta\) and \(X_s\), keep \(W,H\) in the SI acceptance equation, and define \(\hat{\mathbf g}_0,\hat{\mathbf g}\) as nominal/misaligned axis directions rather than arms. The guarded build and rendered page 7 passed. |
| C13 | Put structure factor before multiplicity | AGREE WITH MODIFICATION | DONE | P1 | Section 2.2 defines the conventional crystallographic structure factor and displacement factor before radial-family grouping. SI records the actual isotropic CIF and shared uniaxial implementation scope. The former Cartesian-tensor-as-definition wording and the unnecessary set-builder equation were removed. |
| C14 | Replace \(\mathbf M_\parallel\) with the explicit 2D-powder rule | AGREE WITH MODIFICATION | DONE | P1 | `sections/modelling_methods.tex` and `2D_Supplemental/SI_failure_modes.tex` define \(r=h^2+hk+k^2\), \(Q_R=4\pi\sqrt r/(\sqrt3a)\), lower-case Miller indices, \(Q_R\)-selected profiles, and crystallographic multiplicity as a one-orbit symmetry count distinct from radial-family member count. Accepted members are summed explicitly without a second factor. Both PDFs build, and the static scan finds no active \(\mathbf M_\parallel\) or \(Q_r\). |
| C15 | Reframe the mosaicity introduction | AGREE | DONE | P1 | The opening of `sections/mosaicity_texture.tex` now follows orientation cause -> detector-space consequence -> measured 003/006 observation -> phenomenological model, while separating the near-critical feature and correlated instrument terms. The final main build and rendered mosaicity page verify the revised sequence. |
| C16 | Replace “a limited part” with a quantitative condition | AGREE | DONE | P0 | The vague phrase and incorrect hard-coded angles are removed. The text states that $006$ lies at approximately twice the radial $2\theta$ coordinate of $003$ and uses the successful full wavelength-resolved detector calculation, rather than subtracting incidence from a displayed detector angle, to interpret the radial feature. |
| C17 | Remind the reader that \(\phi\) is azimuth | AGREE | DONE | P2 | `sections/modelling_methods.tex` defines \(\phi\), and the observation and caption in `sections/mosaicity_texture.tex` repeat “detector-plane angle/azimuth \(\phi\).” Unrelated optical/phase symbols were removed or renamed; the final builds and static symbol scan verify the convention. |
| C18 | Demonstrate Gaussian core plus Lorentzian tail quantitatively | AGREE WITH MODIFICATION | BLOCKED | P0 | The moving-Ewald error and necessity overclaims are removed; the broad component is explicitly phenomenological and only adequate within the fitted set. Completion requires direct arrays for a same-fit no-tail counterfactual, a fairly re-optimized Gaussian-only model, multi-angle residuals, widths/tail fraction, correlations, uncertainty, and one declared comparison metric. |
| C19 | Separate \(L\approx0\) intensity from mosaicity | AGREE WITH MODIFICATION | DONE | P0 | `sections/mosaicity_texture.tex` excludes the near-origin feature from mosaic evidence and points to the separate Reflectivity subsection in `sections/correlated_effects.tex`. That subsection states the missing direct arrays, complete prediction, and mosaic treatment explicitly, and the provisional digitized overlay is omitted from the main text. The final main build and source scan verify the separation; a quantitative near-critical result remains a distinct blocked evidence task. |
| C20 | Introduce and justify optical constants | DISAGREE WITH PROPOSED IMPLEMENTATION | DONE | P2 | The main text treats refraction and attenuation as standard corrections and explicitly says they are not a principal result. SI retains the boundary equations, branch convention, attenuation integral, amplitude-versus-flux distinction, and wavelength accumulation. No separate optics sensitivity or optics-off test is included in the present manuscript. |
| C21 | Reframe ordered films as “Test cases:” | DISAGREE WITH PROPOSED IMPLEMENTATION / CHECK | CHECK | P0 | 2026-09-09: the author/current working-tree heading is now “Validation” and was retained. The following records the prior literal-heading closure, which no longer describes the active heading.  The heading in `sections/results_ordered.tex` is literally “Test cases: Bi2Se3 and Bi2Te3 films.” The opening discloses that all \(5^\circ\), \(10^\circ\), and \(15^\circ\) images were fitted and bounds the comparison to selected locations and apparent line shapes; “Validation” and held-out-prediction language were not added. The final main build and heading/language scan verify the change. |
| C22 | Move workflow directly after ordered validation | AGREE | DONE | P0 | `main.tex`, the roadmap in `sections/introduction.tex`, and the opening of `sections/refinement_workflow.tex` now agree on ordered test cases -> refinement workflow -> PbI2 extension. The placement meta-comment was removed; the final input-order scan and main build verify the sequence. |

### Author proof comments - 2026-07-19

The color proof contains exactly three author annotations, all on page 2. These use `U` identifiers so they remain distinct from the audited C01--C22 advisor packet.

| ID | Exact author comment | Disposition | State | Revision and acceptance evidence |
|---|---|---|---|---|
| U01 | “fig 1 b,e, and h need to be reverted to the old figures. Why did you change them?” | AGREE WITH MODIFICATION | DONE | Panel (b), `figures/intro/mono_single_crystal.png`, was already byte-identical to the pre-change asset and needed no edit. The prior curated `reciprocal_3d_powder.png` and `reciprocal_2d_powder.png` assets were restored from `d0a05a7`. With U02 restoring the eight-panel layout, they are again panels (d) and (g). Their Git blobs now match the requested historical versions, and rendered page 2 confirms the visual restoration. |
| U02 | “why did you change figure 1c?” | AGREE | DONE | The single-crystal detector schematic had been added during C02 to complete a 3-by-3 real/reciprocal/detector matrix, even though earlier history deliberately omitted that cell. The schematic was removed, the detector cell was returned to blank, and all later panel labels and references were shifted back to the eight-panel convention. The C02 Ewald-selection explanation remains in the caption and prose. The obsolete schematic asset was deleted. The redraw script was also deleted because its only historical version reproduces the rejected reciprocal-panel replacements rather than the curated assets. The clean isolated build and rendered page 2 passed. |
| U03 | “We should avoid semicolons and never use em-dashes.” | AGREE WITH MODIFICATION | DONE | Prose semicolons were removed from all main-text and SI narrative, captions, lists, and tables by splitting causal or contrasting clauses into sentences. No rendered em dash is present. Mathematical semicolons that separate a function variable from its parameters and source-only TeX/TikZ command terminators were retained because they are notation or syntax, not prose punctuation. Source scans, a 27-page clean isolated main build, an 18-page SI rebuild, and PDF-text inspection passed. |

### Recommended execution order

1. **Recover direct evidence arrays for the blocked work.** Preserve the measured spectrum, refined geometry, direct ordered/mosaic/near-critical arrays, normalization, and plotting scripts together with the accepted configuration that produced them.
2. **Complete the central quantitative evidence gates.** Use the retained spectrum and geometry for the full-vector 003/006 comparison. Run the same-fit no-tail and reoptimized Gaussian-only mosaic comparisons. Regenerate the near-critical result from direct measured and complete calculated arrays only if it remains part of the argument.
3. **Finish ordered-figure provenance.** Record the source arrays, plotting script, image-wise nuisance scales, and whether any displayed amplitudes share a physical scale. Add residual or uncertainty support before stronger quantitative language.
4. **Decide the PbI2 main-text scope.** Keep the transition-matrix extension subordinate until its numerical refit and direct comparison figure are ready.

### Completion protocol

For each current A01-A12 item and each active earlier evidence gate, record the exact revised manuscript/SI location, the evidence or build used to verify it, and one of `TODO`, `IN PROGRESS`, `DONE`, `CHECK`, `BLOCKED`, or `PARKED`. A placeholder, future-tense caption, or related paragraph is not `DONE`. Any disagreement or deliberate non-adoption must be evidence-based and recorded here before the response letter is drafted.

## Core Thesis

The next draft should visibly test whether the forward model quantitatively reproduces measured area-detector diffraction line shapes for Bi2Se3 and Bi2Te3. Quantitative and refinement-quality language is an acceptance target, not a conclusion to assume before the remaining provenance, scaling, residual, and uncertainty checks.

Main claim:

> With the same detector-space coordinate map and finite integration applied to measured and calculated quantities, the displayed Bi2Se3 and Bi2Te3 fitted conditions show close agreement in selected feature locations and apparent line shapes. Claims about common-scale relative intensities, independent prediction, or refinement-quality interpretation remain conditional on verified source arrays, normalization, residuals, and uncertainty.

Use "refinement-quality" carefully. Tie it to plotted data/model overlays, propagated/systematic error, and the structure-factor/CIF/SIF treatment.

## Advisor Philosophy

1. Paper first, software second. Do not make software cleanup or release polish the manuscript bottleneck.
2. Show the strongest supported result clearly. The current central result is close agreement at displayed fitted conditions in selected Bi2Se3 and Bi2Te3 feature locations and apparent line shapes. Strengthen this only after the quantitative evidence gates pass.
3. Write for a diffraction/scattering audience. Use Q, Q_R, Q_z, r-indexed reflection families, explicit (h,k,l) labels where useful, Bragg rods, Bragg positions, reciprocal space, incident angle, mosaicity, finite detector integration, and line-shape fitting.
4. Physics first, optimization later. Put software-native terms such as caked space, internal branches, lookup tables, and optimization details in the supplement unless needed to understand the physics.
5. Figures drive the paper. Draft with enough figures first, then consolidate.
6. Explain more than feels necessary at difficult points: detector space, reciprocal-space trajectories, integration bands, projection, fitting, and simulation.
7. Captions must teach. A caption should explain what is measured, what is calculated, how the comparison was generated, and what conclusion the reader should draw.
8. Show the observational problem before the model correction.
9. Do not undersell the achievement. If justified by overlays and error analysis, careful refinement-quality language is appropriate.
10. Write to teach, not merely to document. The manuscript should read like a clear research talk expanded into paper form.
11. Use the data as the hook: unexpected reflections, the r=0, L=3 star feature, and low-Q / low-L features should motivate mosaicity, bandwidth, and the full detector-space model.
12. Treat explanation itself as a contribution when the geometry is counterintuitive or underexplained.
13. Draft big, then cut. Move implementation-heavy details later, after the main argument is understandable.
14. Define symbols and specialized terms before use, number important equations, and refer to them explicitly.
15. Prefer simple, explicit notation. Keep a reflection-family index distinct from the number of symmetry-equivalent reflections in that family.
16. Preserve causal order and avoid overloaded sentences; at difficult points, give each physical or computational operation its own sentence.
17. Replace vague qualifiers with testable conditions, numerical values, or direct comparisons.
18. When a model component is claimed to be necessary, show an otherwise-identical ablation, residuals, fitted values, and uncertainties where feasible.
19. Keep the strongest completed result ahead of extensions, future capabilities, and implementation detail.

## Primary Story

The paper should follow this logic:

1. Present the experimental diffraction data and physical problem before machinery.
2. Use visible detector images or crops as section springboards.
3. Point out puzzling observations first: off-condition reflections, low-order features, and the r=0, L=3 star feature; present proposed mechanisms only afterward.
4. Explain model ingredients physically: sample geometry, detector geometry, incident angle, beam position, divergence, wavelength spread, mosaicity, bandwidth, structure factor, and resolution.
5. Introduce the two-component mosaicity parameterization early, while treating the broad Lorentzian-like component as phenomenological until controlled comparisons establish necessity.
6. Explain the wavelength-dependent Ewald construction as a tutorial point, with the center and radius rebuilt for every wavelength and incident direction.
7. Separate the r=0, L=3 observation from its proposed mechanism; do not use it as validation evidence until the full-vector calculation and direct arrays are available.
8. Show how data and calculation are projected onto interpretable Q-space trajectories.
9. Demonstrate direct measured/calculated overlays for Bi2Se3 and Bi2Te3.
10. Use verified overlays, propagated/systematic error, and structure-factor treatment before adopting refinement-quality framing.
11. Move computational implementation details to the supplement after the main argument is understandable.
12. Treat PbI2, stacking faults, and diffuse scattering as subordinate until the ordered-film story is clear.

## Current Repo And Manuscript State

| Item | Status | Notes |
|---|---|---|
| `main.tex` scaffold | DONE | Inputs model, mosaic/correlated effects, ordered test cases, refinement workflow, PbI2 extension, and conclusion in that order. |
| Build plumbing | DONE | Uses standard BibTeX via `\bibliography{bibliography/references}`. Generated aux, BBL, log, SyncTeX, and non-review PDFs are ignored. |
| `sections/introduction.tex` | DONE | C01--C03 and the current advisor language corrections are incorporated: layered-film opening, visible Ewald/detector tutorial, bounded fitted-condition accomplishment, narrowed contribution statement, and the final roadmap. |
| `sections/modelling_methods.tex` | DONE | The main text gives the physical sequence, balanced sample/detector geometry, conventional structure-factor notation, and prose family grouping. Detailed geometry and implementation equations are in SI and have been checked against the available executable model. |
| `sections/mosaicity_texture.tex` | IN PROGRESS | The radial wavelength contribution and transverse mosaic contribution are now separated and the successful monochromatic control is stated. Quantitative broad-component necessity remains blocked on C18 arrays/ablations. |
| `sections/correlated_effects.tex` | IN PROGRESS | Wavelength is integrated into the incident-state strategy and the successful full-spectrum control is stated. Optics is reduced to a standard correction. The Parratt-to-kinematic language and joint figure slot are complete, but the final direct-array reflectivity panel remains to be inserted. |
| `sections/results_ordered.tex` | IN PROGRESS | Uses plain fitted-comparison language, defines the finite profile operation, and splits the ordered figure into readable material-specific pages. Direct-source provenance, common-scale interpretation, residuals, and uncertainty remain evidence limitations. |
| `sections/results_diffuse_pbi2.tex` | IN PROGRESS | Reintroduced as an extension; keep subordinate until the ordered-film evidence story is complete. |
| `sections/refinement_workflow.tex` | DONE | Correctly follows the ordered test cases, distinguishes instrument and sample stages, and uses observables consistent with the audited detector measure. |
| `sections/discussion_conclusion.tex` | DONE | Replaced by the author's “Main takeaways” in the 2026-09-09 integration. The prior narrative summarized the bounded fitted-condition result, makes standard optics subordinate, states the evidence limits, and keeps PbI2 as an extension without unique-tail or quantitative-validation overclaims. |
| Ordered-film figures | IN PROGRESS | The readability and caption changes are complete. Direct-array provenance, common scaling, residuals, and uncertainty remain open before stronger quantitative claims. |
| PbI2 figures | IN PROGRESS | Parent-stack schematic complete; reciprocal-space and measured/calculated disorder figures still need completion. |
| Software release polish | PARKED | Paper must demonstrate scientific value first. |

## Active Tasks

### P0 - Next Advisor Draft

| Task | Status | Deliverable | Notes |
|---|---|---|---|
| Finalize Bi2Se3 detector/projection figure | IN PROGRESS | Main-text figure | The full-page layout, descriptive headings, and caption are complete. Verify direct-source provenance, scaling, and uncertainty support before stronger claims. |
| Finalize Bi2Te3 detector/projection figure | IN PROGRESS | Main-text figure | The full-page layout now matches Bi2Se3 and the rendered y-axis is readable. Verify direct-source provenance, scaling, and uncertainty support before stronger claims. |
| Add Bi2Se3 Q_z projection overlays | IN PROGRESS | Figure panels | Existing overlays are enlarged and explained. Verify direct-source provenance, normalization, residuals, and uncertainty. |
| Add Bi2Te3 Q_z projection overlays | IN PROGRESS | Figure panels | Existing overlays are enlarged and explained. Verify direct-source provenance, normalization, residuals, and uncertainty. |
| Define reflection-family labels | DONE | Figure/caption convention | Main text and SI define lower-case \((h,k,l)\), \(r=h^2+hk+k^2\), \(Q_R\), \(Q_z\), and \(L\), with family member count separated from one-orbit multiplicity. |
| Add early two-component mosaicity evidence | BLOCKED | Text + controlled comparison | Observation, model, and limitation are now separated; direct arrays are still required for the matched no-tail and re-optimized Gaussian-only comparisons under C18. |
| Verify ordered-figure provenance and scaling | BLOCKED | Reproducible figure record | Supply scripts/direct arrays; state normalization and whether scales are common across families/images; add residual/uncertainty support before claiming relative-intensity validation. |
| Correct moving-center bandwidth geometry | DONE | Main/SI physics interface | Main text and SI rebuild the Ewald center and radius for every wavelength/direction sample and reject a universal inverse-order rule. |
| Test the low-L bandwidth mechanism directly | DONE | Full-spectrum versus mean-wavelength control | The otherwise-identical control succeeds: the full spectrum reproduces the radial $003$ streak, the mean-wavelength calculation collapses it, and the transverse $\phi$ width remains. Main Secs. 2.3-2.4 and SI Sec. S3 record the result. |
| Verify 003/006 angular wording and interpretation | DONE | Corrected text + full detector calculation | The incorrect hard-coded angles were removed. The manuscript states that $006$ occurs at approximately twice the radial coordinate of $003$ and uses the full wavelength-resolved detector calculation, not naive angle subtraction, to explain their contrast. |
| Bound the role of optical constants | DONE | Main/SI scope decision | Optics is retained as a standard correction, described as strongest near critical-angle conditions, and moved chiefly to SI. No separate sensitivity or optics-off test is included. |
| Add joint low-$Q_z$/003 specular comparison | IN PROGRESS | Direct-array main-text figure | The Parratt-to-kinematic terminology and figure slot are in place. Insert the direct measured and complete calculated profile on one coordinate map, background treatment, and intensity scale, including the expanded full-spectrum versus mean-wavelength $003$ panel. |
| Reorder workflow before PbI2 | DONE | `main.tex` + workflow opening | Ordered test cases now lead directly to the refinement workflow, then to the PbI2 extension; the placement meta-comment is removed. |
| Confirm ordered-section “Test cases:” intent | DONE | Heading decision | The heading is literally “Test cases: Bi2Se3 and Bi2Te3 films”; no “Validation” or held-out claim was added. |
| Decide next-draft PbI2 scope | DONE | Main-text/SI boundary | The 2026-09-09 author revision retains a condensed physical account and four-stage comparison hierarchy. The detailed transition-matrix construction remains in SI, and the comparison panels remain explicit placeholders pending refit/direct arrays. |
| Remove ambiguous “grown by TEM” | DONE | Results wording | The phrase is removed; no replacement growth method was invented without verified provenance. |
| Write Q-space/projection caveat | DONE | Methods/results paragraph | Results state why projected Q_z is not an ideal perfect-crystal cut and why data/model comparison remains meaningful. |
| Disclose experimental incident angles | DONE | Ordered-results prose | The ordered section states that images at \(5^\circ\), \(10^\circ\), and \(15^\circ\) were included in refinement and distinguishes the displayed \(5^\circ\) comparison. |
| Replace remaining future-tense scaffold prose | DONE | Results prose | Built main-text and SI prose describe the present figures and current evidence boundaries directly. Placeholder macros remain only as safe build fallbacks when an image file is absent. |

### P1 - Complete-Draft Support

| Task | Status | Deliverable | Notes |
|---|---|---|---|
| Consolidate 2theta-phi/caked implementation supplement | DONE | Built supplement section | Unique material was merged into `2D_Supplemental/SI_failure_modes.tex`; the competing standalone outline was deleted. |
| Explain sub-pixelation/binning | DONE | Supplement section | SI defines the continuous detector density, native-pixel or finite-angular-bin integration, signal/normalization accumulation, and uncertainty propagation. |
| Explain weighted source-state quadrature | DONE | Supplement section | SI defines the seeded equal-mass antithetic Latin-hypercube construction in two positions, two directions, and wavelength, together with convergence and provenance requirements. |
| Explain continuous mosaic evaluation | DONE | Supplement section | SI states that mosaicity remains a continuous angular density and that wavelength-specific Ewald roots are solved analytically. No stochastic crystallite-orientation cloud is deposited. |
| Consolidate notation | DONE | Main text + SI convention | Built sources use \(Q_R\), lower-case Miller indices, \(p_{\mathrm{tail}}\), distinct phase/index symbols, and explicit family-versus-multiplicity language. |
| Complete geometric-correction SI chain | DONE | Supplement section | Sample and detector intersections, rigid detector mapping, active-area conditions, finite detector integration, moving wavelength, and standard planar-interface corrections are present and checked against the available implementation. |
| Repair sample-geometry Figure 4 | DONE | Main-text figure + caption | Figure/caption define \(\mathbf r_{\mathrm{hit}}\) and axis directions, remove \(X_s\) and the undefined \(\delta\), and place \(W,H\) in the SI acceptance equation. |
| Add h-BN fitting method | TODO | Supplement section | Unique workflow step mentioned in the paper. |
| Assemble full ordered-film peak-profile checks | BLOCKED | Supplement figures | Direct measured/model arrays and plotting provenance are required for expanded panels, residuals, and uncertainty. |
| Finalize structure-factor/CIF/SIF and occupancy discussion | IN PROGRESS | Main text plus SI | The conventional structure-factor and verified isotropic/shared-uniaxial implementation scope are explicit. The final fitted structural file, occupancy provenance, and any future general anisotropic-tensor support remain to be documented before numerical refinement claims. |
| Add propagated/systematic error support | IN PROGRESS | Main text plus SI | Main text gives the interpretation boundary; SI carries derivation. |

### P2 - Parked

| Task | Status | Reason |
|---|---|---|
| PbI2 diffuse scattering / stacking-fault finalization | IN PROGRESS | Keep subordinate to the ordered-film evidence story. |
| PbI2 selected-rod comparison figures | TODO | Placeholder exists; generate a direct-source figure later. |
| Software release cleanup | PARKED | Paper first. |
| Full software feature documentation | PARKED | Useful later, not central to current draft. |

## Immediate Checklist

- [x] Complete the Bi2Se3 main-text layout and readability review. Direct-array provenance remains a separate open evidence task.
- [ ] Recover Bi2Se3 direct plotting arrays, scaling record, residuals, and uncertainty support.
- [x] Complete the Bi2Te3 main-text layout and rendered y-axis readability review. Direct-array provenance remains separate.
- [ ] Recover Bi2Te3 direct plotting arrays, scaling record, residuals, and uncertainty support.
- [x] Initial r or explicit (h,k,l) reflection-family label convention.
- [x] Paragraph explaining imperfect Q-space/projection interpretation.
- [x] Paragraph and figure concept for the two-component orientation distribution, with explicit separation between geometric sensitivity and ablation evidence.
- [x] State that the \(5^\circ\), \(10^\circ\), and \(15^\circ\) Bi2Se3/Bi2Te3 images were included in refinement and identify the displayed \(5^\circ\) comparison.
- [x] Merge the standalone implementation outline into the built SI and consolidate notation.
- [x] Verify the sample/detector intersections, detector mapping, calculation order, and source-versus-mosaic integration against the available implementation.
- [x] Correct the wavelength construction so each sample moves both the Ewald-sphere center and radius; remove the universal inverse-order claim.
- [x] Test the low-$L$ bandwidth mechanism with the full-spectrum versus mean-wavelength detector calculation; the successful control assigns the radial $003$ streak primarily to wavelength dispersion.
- [ ] Fair Gaussian-only refit, same-fit no-tail counterfactual, and Gaussian-plus-Lorentzian comparison across multiple angles with residuals/correlation/uncertainty.
- [x] Remove the incorrect hard-coded 003/006 angles, retain the approximately two-to-one radial-coordinate relation, and interpret the features with the full wavelength-resolved detector calculation rather than naive angle subtraction.
- [x] Physics-check the optical boundary, depth attenuation, and amplitude/flux interface, move the derivation to SI, and state that optics is not a principal contribution.
- [x] Keep optics subordinate as a standard correction and omit a separate optics sensitivity or optics-off test from the present manuscript.
- [ ] Direct-array measured/full-model near-critical plot replacing the digitized/repaired limiting traces.
- [x] Move the workflow directly after ordered-film test cases and before PbI2.
- [ ] Recheck C21: the 2026-09-09 author/current working-tree heading is “Validation”; the prior literal “Test cases:” closure no longer applies.
- [x] Adopt the author's condensed PbI2 narrative and planned four-stage hierarchy (2026-09-09). Numerical refit and direct comparison panels remain pending.
- [x] Remove “grown by TEM” without inventing a replacement growth method.

## Advisor Questions

- Is the proposed reflection-family labeling convention clear enough?
- Which Bi2Se3/Bi2Te3 incident angles should be main-text figures versus supplement?
- Should Lorentzian-tail evidence be a standalone figure or part of the mosaicity figure?
- How many Q_z projection panels are enough for the main text?
- Did “Test cases:” on page 14 request that exact heading prefix, or only validation framing?

## Figure Guidance

Figures must show data. Do not obscure measured detector intensity with too many Bragg-position circles, colored paths, markers, or labels.

Quantitative figures must be generated from retained direct data/model arrays with documented scripts and normalization. Digitized, repaired, clipped, or placeholder curves are provisional and cannot support fits, residuals, ablations, or validation claims.

Each main-text figure caption should state:

1. what material is shown;
2. what is measured and what is calculated;
3. what solid lines, dashed lines, boxes, rods, projection bands, and symbols mean;
4. what coordinate convention is being used;
5. how projected profiles were generated;
6. what feature the reader should notice;
7. how the figure supports the manuscript claim.

### Active Figure Status

| Figure / asset | Status | Purpose |
|---|---|---|
| `figures/intro/` | DONE | Fig. 1 uses the restored eight-panel layout and the requested curated reciprocal-space assets. Panel (b) was already unchanged. The added single-crystal detector schematic and the nonmatching redraw script were removed. The caption and prose still map single-crystal points to sparse spots, 3D shells to rings, and 2D-powder reciprocal rings to paired detector features. Rendered page 2 was checked. |
| `figures/results_ordered/00L_region_marked.png` | IN PROGRESS | Built low-L r=0 observation crop; it is explicitly excluded from broad-mosaic evidence pending direct near-critical arrays and mechanism testing. |
| `figures/results_ordered/figure7_bi2se3_qr_rod_qz_profiles_detector_selected_q_regions_5deg.png` | IN PROGRESS | Main-text readability and caption integration are complete. Direct-source generation provenance remains open. |
| `figures/results_ordered/figure7_bi2te3_qr_rod_qz_profiles_detector_selected_q_regions_5deg.png` | IN PROGRESS | Main-text styling and readability now match Bi2Se3. Direct-source generation provenance remains open. |
| `figures/results_ordered/figure7_bi2se3_qr_rod_qz_profiles.png` | IN PROGRESS | The enlarged overlay passes rendered axis/label review. Direct arrays, scaling provenance, residuals, and uncertainty remain open. |
| `figures/results_ordered/figure7_bi2te3_qr_rod_qz_profiles.png` | IN PROGRESS | The enlarged overlay passes rendered axis/label review. Direct arrays, scaling provenance, residuals, and uncertainty remain open. |
| `figures/correlated_effects/fig_reflectivity_data_parratt_kinematic.png` | CHECK | Provisional only: digitized/repaired traces, clipped-branch plateau, and no full blended prediction. Replace with direct exports before using as evidence. |
| `figures/mosaic/lorentzian_tail_evidence.png` or existing ordered-film crop | TODO | Direct evidence that narrow-core mosaicity alone is insufficient. |
| `figures/mosaic/moving_ewald_wavelength_series.png` | BLOCKED | Planned wavelength-by-wavelength moving-center/radius schematic or direct simulation; requires the verified spectrum/model inputs before it can support a mechanism claim. |
| `figures/mosaic/low_q_003_03_06_data_model_overlay.png` | BLOCKED | Planned measured/calculated overlay or control comparison; direct arrays and the verified full-vector calculation are missing. |
| `figures/results_pbi2/transition_matrix/pbi2_polytype_stacks.{tex,png}` | DONE | Figure 11 parent-stack schematic in rF_sigma notation. |
| `figures/results_pbi2/pbi2_raw_detector_diffuse_motivation.png` | TODO | PbI2 measured detector image with diffuse features. |
| `figures/results_pbi2/pbi2_diffuse_data_model_overlay.png` | TODO | Measured/ordered/faulted/residual projection figure. |

Recommended main-text sequence:

1. Orientational limits / 2D powder motivation.
2. Model geometry and mosaicity.
3. Low-L r=0 star-feature observation, explicitly separated from any mechanism claim until direct full-vector tests are available.
4. Bi2Se3 ordered-film detector setup and Q_z overlays.
5. Bi2Te3 ordered-film detector setup and Q_z overlays.
6. Refinement workflow and uncertainty/structure-factor framing.
7. PbI2 extension only if it does not compete with the ordered-film evidence story.

## Projection And Coordinate Guidance

Use Q_z as the main horizontal/projected coordinate for line profiles when that is the physical interpretation. Use Q_R or scalar r to identify which reciprocal-space trajectory is sampled.

The manuscript should say:

- data are projected along trajectories corresponding to fixed Q_R or r-family conditions;
- profiles are reported versus Q_z;
- mosaicity, beam divergence, wavelength spread, incidence-angle uncertainty, detector mapping, and finite integration make the projected observable a finite-region measurement rather than an ideal reciprocal-space line cut;
- calculation and data pass through the same coordinate transformation, finite integration limits, and normalization;
- therefore the measured/calculated comparison is meaningful and is the central method test.

Do not overstate Q-space exactness. If a plotted reciprocal-space trajectory omits sample rotation, chi, or alignment distortions, describe it as an approximate projection guide rather than a physical explanation of peak offsets.

Suggested concise caveat:

> The horizontal coordinate is reported as Q_z along the selected trajectory. Because mosaicity, divergence, wavelength spread, incidence-angle uncertainty, and detector sampling broaden the effective scattering condition, these profiles are not ideal reciprocal-space cuts. The comparison remains direct because the same coordinate transformation and finite integration operation are applied to the calculated and measured detector quantities.

## Indexing And Labels

Use labels that a diffraction reader can interpret.

For hexagonal Bi2Se3 and Bi2Te3, use the compact scalar reflection-family label:

```tex
r = h^2 + h k + k^2 .
```

Define r before use. Use r=1, r=3, or explicit (h,k,l) labels when space allows. Avoid relying only on internal labels such as M1, M2, plus branch, or minus branch unless they are explicitly tied to physical reflection families.

If symmetry-equivalent reflections contribute to the same ring/manifold, state that convention in the caption or text. Q_R values may be secondary numerical information, not the primary label.

## Two-Component Mosaicity

The two-component mosaicity model must appear early and be supported by visible evidence.

Core argument:

- Off-condition reflections motivate testing a component broader than the narrow mosaic core.
- The current Gaussian-like-core plus Lorentzian-like-broad-component parameterization is an adequate phenomenological description within the fitted set, not proof of a unique physical distribution.
- Sensitivity to the broad component is coupled to wavelength bandwidth, divergence, alignment, and background.
- Near-critical low-\(L\) intensity additionally depends on optical response and is excluded from evidence for the broad component.
- Necessity may be claimed only after a same-fit no-tail counterfactual and a fairly re-optimized Gaussian-only comparison show worse residuals across multiple angles, with fitted values, correlations, and uncertainty.

The compact parameterization belongs in the main text because it states the tested physical hypothesis; the controlled evidence and implementation detail can be expanded in SI.

## Bandwidth / Bragg-Sphere / Mosaic-Cap Tutorial

This is a conceptual contribution and should be explained step by step without assigning the low-\(L\) mechanism prematurely.

Required explanation:

1. Define the incident wavevector, exit wavevector, reciprocal-lattice vector, Ewald sphere, and elastic intersection condition.
2. For every wavelength and incident-direction sample, rebuild both the Ewald-sphere center \(-\mathbf k_i\) and radius \(2\pi/\lambda\).
3. Explain that allowed intersections are not populated uniformly; intensity also depends on the mosaic distribution, beam divergence, structure factor, optics, and detector acceptance.
4. Use the Bragg-law derivative only as a limiting sanity check, not as a universal inverse-order or cap-overlap law.
5. Pair schematic examples with calculated detector images only after the spectrum and executable geometry are verified.
6. Then compare measured and calculated detector-space consequences using direct arrays.

The r=0, L=3 star-like or line-like feature should be shown as an observation. Its bandwidth/mosaic interpretation remains a CHECK until the measured spectrum and full vector calculation reproduce the feature and a controlled reflection/optics comparison isolates the mechanism.

## Experimental Section Requirements

The manuscript must state what data were collected, not only what data are shown.

For each material, include:

- incident angles collected;
- incident angles shown in the main text;
- incident angles moved to the supplement;
- why the shown angles are representative or useful.

Do not let the reader infer that only displayed incident angles were measured.

## Workflow Guidance

The workflow table is useful, but it cannot stand alone. A good workflow section should:

- say that many parameters must be established before quantitative comparison is possible;
- distinguish instrumental parameters from sample-dependent parameters;
- explain the sequence used to determine those parameters;
- describe what each step contributes to the final comparison;
- avoid making the reader infer the method from a table alone.

Placement matters. The workflow should not interrupt the physical story; it belongs after the model/results context makes the staging valuable.

## Supplement Scope

Main text should contain:

- physical motivation;
- main experimental data;
- model ingredients in physical language;
- two-component mosaicity argument;
- Q / Q_R / Q_z projection concept;
- Bi2Se3 and Bi2Te3 data/calculation overlays;
- enough experimental detail to understand shown data;
- the mosaicity equation or equivalent explanation;
- r=0, L=3 star-feature explanation;
- propagated/systematic error at the interpretation level;
- structure-factor/CIF/SIF and occupancy-change discussion needed for refinement-quality framing.

Supplement should contain:

- 2theta-phi implementation details;
- caked coordinate transformation;
- sub-pixelation and binning;
- lookup-table or detector-remapping details;
- weighted source-state quadrature details;
- beam position, divergence, and wavelength-state construction;
- continuous mosaic-density evaluation and analytic Ewald restriction;
- computational efficiency/optimization arguments;
- full profile arrays or large peak-by-peak fit collections;
- h-BN fitting procedure if part of the analysis pipeline;
- additional incident-angle data;
- full CIF/SIF contents if too long for main text;
- full propagated-error derivations.

### Proposed Supplement Outline

The standalone implementation outline has been merged into `2D_Supplemental/SI_failure_modes.tex` and deleted. The built SI is the only active supplement source; the statuses below distinguish written interfaces from missing executable or numerical evidence.

| Section | Status | Contents |
|---|---|---|
| S1 Geometric acceptance and detector mapping | DONE | Sample-plane acceptance, optional finite support, detector rigid transform, tilted-plane intersection, continuous pixel coordinates, active-area tests, and calculation order are written and implementation-checked. |
| S2 Crystallographic displacement conventions | DONE | Conventional structure-factor notation, isotropic U/B conversion, current CIF input scope, and shared uniaxial film-frame damping are documented. |
| S3 Wavelength sampling and moving Ewald construction | DONE | Weighted position/direction/wavelength states, moving center and radius, convergence checks, and the analytic Ewald restriction are documented. |
| S4 Staged workflow, projection, uncertainty, optics, and near-critical diagnostic | DONE | The finite detector observable, uncertainty propagation, continuous mosaic density, standard interface correction, and provisional direct-array limitation are documented. |
| S5 Transition-matrix derivation | IN PROGRESS | The symbolic PbI2 construction and deterministic checks are complete. Experimental transition-matrix refit values and a direct selected-rod comparison remain open. |
| S6 Two-component mosaicity checks | BLOCKED | Direct arrays are required for same-fit no-tail and re-optimized Gaussian-only comparisons with residuals, parameters, correlations, and uncertainty. |
| S7 Full ordered-film peak profiles | BLOCKED | Expanded direct-source Bi2Se3/Bi2Te3 profiles, residuals, scaling, and uncertainty require the missing arrays/scripts. |
| S8 h-BN fitting method | TODO | What was fit, what parameters were extracted, and why it belongs in the workflow. |

## Concision Guidance

Cut or move text that mainly explains software convenience, implementation bookkeeping, broad literature context, or parameter auditing before the fit evidence appears.

Keep text that helps a diffraction reader see:

1. what was measured;
2. what physical effect is modeled;
3. how measured and calculated intensities are compared;
4. what the current Bi2Se3/Bi2Te3 overlays support and which quantitative claims still require provenance, residuals, and uncertainty.

Highest-priority cuts before the next advisor draft:

1. Compress the introduction's material-family survey.
2. Shorten the post-Fig. 1 orientational-limits paragraph.
3. Reduce workflow prose because the table already carries the staged refinement.
4. Move or shrink the ordered structure-parameter table.
5. Convert future-tense results text into direct result language once figures are final.
6. Keep PbI2 from competing with the ordered-film evidence story.
7. Remove final-discussion meta-comments about how the paper should be organized.
8. Add the controlled broad-component comparisons required by C18 instead of compensating with more prose.

## Decision Log

| Date | Decision / change | Current interpretation |
|---|---|---|
| 2026-08-06 | Assigned the radial $003$ streak with a successful wavelength control and reframed the specular transition. | Main Secs. 2.3-2.4 and SI Sec. S3 now distinguish radial $2\theta$ wavelength broadening from transverse $\phi$ mosaic broadening. The incorrect fixed angles are removed, the full-spectrum versus mean-wavelength result is stated, optics remains a subordinate SI correction without a sensitivity test, "Parratt-to-kinematic crossover" replaces the unclear phrase, and a joint low-$Q_z$ through $003$ figure slot is inserted. |
| 2026-08-06 | Implemented the immediately resolvable current advisor comments and completed an independent implementation-parity audit. | The main text now teaches the physical sequence, balances sample and detector geometry, uses conventional crystallographic notation, defines the matched finite profile observable, splits the ordered-film figure for readability, and treats optics as a standard SI correction. Controlled mosaic ablation, ordered-figure provenance, and the final direct-array reflectivity panel remain evidence gates. |
| 2026-07-30 | Added a dependency-only Overleaf export with persistent graphics caching. | `scripts/export-overleaf.ps1` packages both document entry points, losslessly wraps all referenced raster assets as PDFs, pre-renders live TikZ figures with overflow-safe bounds, removes unused staged graphics packages, compile-checks the main manuscript and SI, and creates `build_overleaf/overleaf.zip`. Git metadata, scripts, existing review PDFs, unreferenced assets, and LaTeX auxiliaries are excluded. The generated package preserved the 27-page main layout in rendered comparison; a clean local check completed in 9.11 s for the main manuscript and 8.19 s for the SI. |
| 2026-07-19 | Addressed all three author annotations in `main_advisor_comment_color_proof.pdf`. | Restored the earlier eight-panel Fig. 1 design and curated 3D/2D reciprocal panels, removed the reintroduced panel (c), retained the C02 diffraction-selection explanation, and removed prose semicolons and em dashes throughout the main manuscript and SI. Mathematical semicolon separators and source-only TeX syntax remain. The desired reciprocal assets were introduced as curated binaries in `9bb1c9b` and losslessly re-encoded in `bfb7869`. No historical generator reproduces them, so the misleading redraw script was removed. Both PDFs build, and revised Fig. 1 passed rendered review. |
| 2026-07-19 | Restyled paragraph-level headings as unnumbered blocks in the main manuscript and SI. | The shared preamble treatment is available to the main-text physical-foundation material and the SI. The four existing SI optical-interface subheadings retain semantic `\paragraph` hierarchy but appear on their own lines with book-like vertical spacing; numbering remains limited through `\subsubsection`. |
| 2026-07-18 | Completed the manuscript-resolvable advisor-correction pass. | C01--C04, C09, C12--C15, C17, C19, C21, and C22 are DONE; C05--C08 and C10 remain IN PROGRESS for executable/configuration parity; C11, C16, C18, and C20 are BLOCKED on specified missing inputs. Main/SI notation, Ewald/optical interfaces, causal ordering, workflow placement, and evidence boundaries are reconciled and verified by final builds, static scans, and rendered-page review. |
| 2026-07-18 | Resolved advisor comment C04 at its literal scope. | Defined \(\mathbf k_i\), \(\mathbf k_f\), \(\mathbf Q\), and \(\mathbf G\) immediately before use and numbered/cross-referenced the four associated diffraction equations. C04 itself introduced no frame superscripts or rotation-matrix rewrite; separate geometry-interface work for C05--C08, C10, and C11 supplies the broader SI treatment. The final build and rendered Methods page passed. |
| 2026-07-18 | Rewrote the C03 accomplishment and roadmap close. | Replaced the single capability-heavy paragraph with a prior-art transition, an evidence-bounded accomplishment paragraph, and a roadmap matching the final manuscript order. The ordered Bi2Se3/Bi2Te3 result was bounded to selected detector-feature locations and apparent profile line shapes rather than quantitative or predictive validation. PbI2 appears only as a separate extension. C03 is DONE after the C22 order and evidence boundary were reconciled and the main PDF rebuilt. |
| 2026-07-18 | Resolved advisor comment C01. | Replaced the WAXS-first opening with a 115-word layered-film-first paragraph, defined the 2D-powder state operationally, made substrate/growth dependence explicit, removed the material-family catalogue, and moved the orientational-limits comparison ahead of the PbI2 overview. The guarded build succeeded and rendered pages 1-3 passed visual review. |
| 2026-07-18 | Audited the 22-comment Miceli packet and integrated the writing philosophy into `AGENTS.md`. | All comments are traceable here. Ten packet responses are accepted, ten require modification, and two proposed implementations are rejected/replaced while retaining the advisor's underlying concern. Critical physics/provenance checks now precede prose polish; at the time of this initial audit, no manuscript comment was fully closed. |
| 2026-06-29 | Added Smilgies/Li indexGIXS citation and narrowed detector-space framing. | Smilgies and Li are now cited as prior art for calculating/overlaying grazing-incidence diffraction-spot positions directly in detector space. The manuscript contribution is framed as detector-space intensity/profile forward modeling after the geometric indexing step, not as novelty in detector-space indexing itself. |
| 2026-06-25 | Pruned generated figure diagnostics. | Removed 76 unreferenced generated PbI2 assets: misplaced ordered-folder outputs, parent-level generated exports, and low/medium/high `profiles/` diagnostics. Active manuscript and supplement figure paths remain the source contract for retained assets. |
| 2026-06-25 | Integrated annotated-PDF changes. | Historical state, superseded 2026-07-18: the digitized near-critical overlay was removed from the main text because it lacked the complete prediction and direct arrays. The observation remains separated from mosaic evidence; any future quantitative near-critical result requires direct exports and a declared mosaic treatment. |
| 2026-06-25 | Consolidated legacy manuscript tracking. | Advisor notes, meeting-prep notes, figure status, supplement status, changelogs, TODO tracking, old patch handoffs, and temporary review artifacts are subsumed here or removed. Git history remains the archive for exact old wording. |
| 2026-06-23 | Replaced superseded PbI2 scalar slip/flip model. | PbI2 now uses a six-state direction-resolved transition-matrix model. Numerical epsilon_j and population weights remain pending a refit and must not be converted from old scalar p values. |
| 2026-06-23 | Replaced PbI2 parent-stack Figure 11. | The projected 2H/4H/6H atomic-stack schematic is integrated with rF_sigma notation and a teaching caption. |
| 2026-06-23 | Updated PbI2 intensity equations. | Main text uses parent probability 1-epsilon_j and alternatives epsilon_j/4, includes finite-stack self-plus-pair intensity, and keeps the full derivation in SI. |
| 2026-06-15 | Fixed clean-build BibTeX failure. | Clean builds should regenerate bibliography from tracked sources; watcher processes must be stopped manually before guarded rebuilds. |
| 2026-06-15 | Added GitHub Actions LaTeX build check. | CI checks root `main.pdf`, `build/main.aux`, and non-empty `build/main.bbl` with bibliography markers. |
| 2026-06-11 | Removed legacy build products and patch handoffs. | Active manuscript sources, current figure assets, bibliography source, `main.pdf`, and `2D_Supplemental/SI_failure_modes.pdf` are retained review artifacts. |
| 2026-05-22 | Added bandwidth-scaling and uncertainty material. | Superseded first on 2026-07-18 and again on 2026-08-06: the fixed-center inverse-order argument was removed, and the later full-spectrum versus mean-wavelength control assigned the radial $003$ streak primarily to wavelength dispersion. Projection/intensity uncertainty formulas are retained. |
| 2026-05-12 | Advisor-feedback restructure. | Low-L observation remains before the ordered test cases; correlated effects remain separate; PbI2 remains an extension. Superseded placement: the workflow now follows the ordered test cases directly and precedes PbI2. |
| 2026-05-10 | Moved low-L r=0 star feature before ordered-film validation. | Observation now precedes the fit-quality payoff. |
| 2026-04-30 | Advisor meeting. | Paper first, ordered Bi2Se3/Bi2Te3 figures first, direct Q_z overlays, supplement for implementation, PbI2 parked/subordinate. |
| 2026-04-30 | Added preliminary literature-bounded parameter citations. | Fitted structural values remain provisional and must be checked against final refinement output before submission. |
| 2026-04-26 | Cleaned language and fit layout. | Historical state, superseded 2026-07-18: the selected-rod polar angle formerly used eta; active SI now uses \(\vartheta_Q\), and unrelated phase symbols use distinct notation. |
| 2026-04-21 | Added Nookiin related-work citation. | Nookiin is complementary atomistic/supercell context, not a workflow dependency or software used by the present model. |
| 2026-03-13 | Pedagogical diffraction-model rewrite. | The model builds from ideal scattering geometry to experimental complications and weights. |
| 2026-03-03/04 | Advisor-annotation passes. | Section structure, notation, figure plumbing, beam/sample components, and supporting information were updated; detailed line-by-line history remains in git. |

## Current Change Status

| Area | Status | Notes |
|---|---|---|
| Feature | DONE | Implemented the immediately resolvable 2026-08-06 advisor comments across the main text and SI. The model narrative now follows incident state to sample acceptance, Ewald restriction, exit propagation, tilted-detector mapping, and matched finite integration. |
| Bug/error | DONE | Corrected the source-state versus mosaic treatment, conventionalized displacement notation, removed the unnecessary family-set equation, assigned the radial $003$ streak with the successful wavelength control, and replaced the unclear specular-transition terminology. |
| Build/QA | DONE | Main and SI PDFs rebuild without undefined references, undefined citations, overfull boxes, or clipped content. Rendered pages were reviewed after the edit. |
| Figure revision | DONE | The ordered-film composite is split into material-specific pages with larger panels, descriptive headings, and captions that define the profile construction and claim boundary. Direct-array provenance remains a separate evidence task. |
| Documentation | DONE | This canonical file records A01--A12, the successful wavelength-control result, the no-sensitivity-test optics decision, and the remaining mosaic/provenance/reflectivity figure gates. No competing status document was created. |
| Shipping/rollback | DONE | No external release was published or pushed. The manuscript and SI review PDFs were rebuilt locally. |

## Build And Reset Recovery

The main manuscript uses `latexmkrc` to write the public artifact to repository-root `main.pdf`. Generated LaTeX auxiliaries, logs, and bibliography outputs go into `build/`, which is ignored by git.

For an Overleaf upload, generate the clean cached package from the repository root:

```powershell
.\scripts\export-overleaf.ps1
```

The script creates `build_overleaf/overleaf/` and `build_overleaf/overleaf.zip`. Upload the ZIP contents with `main.tex` at the Overleaf project root. The package contains only the dependency closure for `main.tex` and `2D_Supplemental/SI_failure_modes.tex`; referenced TikZ and raster graphics are losslessly cached as PDFs. Persistent source-hash caches under `build_overleaf/graphics-cache/` make later exports rebuild only changed graphics. The default command compile-checks both entry points before creating the ZIP. Use `-SkipCompileCheck` only when a faster package-only refresh is intentional.

Build the complete Supporting Information and its appended note from the repository root:

```powershell
.\scripts\build-supplement.ps1
```

The helper retains isolated build state under `build_codex/supplement/`. Use `-NoteOnly` to refresh only the appendix dependency. The Overleaf exporter runs this dependency build automatically.

Use the guarded clean rebuild when citations or bibliography output look stale:

```powershell
.\scripts\build-main.ps1
```

If VimTeX or another editor watcher is already running, the script stops before touching build state and prints matching process IDs. Stop the watcher in the editor or process manager, then rerun the script. The script intentionally does not kill watcher processes because Windows process command lines do not reliably prove which repository owns a generic `main.tex` watcher.

Underlying commands:

```powershell
latexmk -c main.tex
latexmk -pdf -g -interaction=nonstopmode -file-line-error main.tex
```

After a successful build, root `main.pdf` is the review artifact and GitHub Pages PDF. `build/main.aux` should contain `\bibstyle{apsrev4-2}` and `\bibdata{bibliography/references}`. `build/main.bbl` should be non-empty and contain `\begin{thebibliography}`. There should not be a second tracked `main.pdf` under any subdirectory.

If VimTeX or another editor reports `I found no \bibdata command` after a reset, first stop the continuous build watcher, then run the clean rebuild above. That BibTeX message usually means BibTeX read an incomplete or stale `.aux`; it does not by itself prove that `main.tex` is missing bibliography commands.

## Revision Checklist

Before returning a manuscript edit or new section, check:

- Does the text put the paper before software?
- Does it explain physics before optimization?
- Does it use Q, Q_R, Q_z, r-indexed labels, and reciprocal-space language where appropriate?
- Does it avoid unnecessary software-native terminology in the main text?
- Does it make the measured data and calculated fit comparison clear?
- Does it state only the currently supported fitted-condition location/apparent-line-shape result and identify what is still required for quantitative or refinement-quality claims?
- Does it introduce the two-component mosaicity hypothesis early without calling the broad component necessary before the C18 controls exist?
- Does it label reflections in a way a diffraction reader can understand?
- Does the caption teach the reader what to notice?
- Does it show data before explaining the model correction?
- Does it explain that wavelength sampling moves both the Ewald-sphere center and radius, without using a universal inverse-order or cap-overlap rule?
- Does it avoid overclaiming that Gaussian/Lorentzian forms are uniquely true?
- Does it distinguish the radial $r=0$, $L=3$ wavelength result from the transverse mosaic observable and tie the assignment to the full-spectrum versus mean-wavelength control?
- Are sample-orientation effects included in plotted trajectories, or is the limitation stated?
- Are Q_R, Q_z, L, r, and explicit (h,k,l) labels defined before use?
- Does main text discuss propagated/systematic error rather than hiding it entirely in SI?
- Does the manuscript distinguish the verified isotropic/shared-uniaxial structure-factor implementation from the still-unreported final fitted file and occupancy provenance?
- Is refinement-quality language withheld until source provenance, scaling, residual, and uncertainty evidence supports it?
- Are the Ewald vectors, reflection-family labels, multiplicity, angles, and specialized operations defined before use, with important equations numbered?
- Have vague qualifiers been replaced by a testable condition, value, range, or controlled comparison?
- Does narrative and caption prose avoid semicolons and em dashes while retaining semicolons that serve a mathematical or TeX-syntax role?
- If a component is called necessary, does an otherwise-identical ablation with residuals or a documented reason for not running one support that claim?
- Does `main.tex` place ordered-film test cases before the refinement workflow and PbI2 extension?
- Does every current A01-A12 item and each still-active older evidence gate have an exact change location, verification evidence, and honest status?

## Document Policy

Do not recreate separate advisor-note, meeting-prep, figure-status, supplement-status, input-inventory, TODO, or changelog documents unless explicitly requested. Add new manuscript guidance, active status, open questions, and durable decision summaries here instead.
