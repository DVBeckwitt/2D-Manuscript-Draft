[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "rasim-next"
version = "0.0.0"
description = "Greenfield native-detector X-ray scattering and staged refinement core"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
  "gemmi>=0.7,<0.8",
  "numba>=0.66,<0.67",
  "numpy>=2.0,<3",
  "packaging>=24,<27",
  "PyYAML>=6.0.3,<7",
  "scipy>=1.14,<2",
  "xraydb>=4.5,<5",
]

[project.optional-dependencies]
visualization = [
  "matplotlib>=3.9,<4",
  "Pillow>=12,<13",
  "PySide6-Essentials>=6.8,<7",
]

[dependency-groups]
dev = [
  "Pillow>=12,<13",
  "pytest>=8,<10",
  "ruff>=0.12,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/painted_ewald", "src/rasim_next"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]
